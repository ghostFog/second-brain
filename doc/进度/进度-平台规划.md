# 平台规划 · 已完结进度

> 仅归档已完结条目；进行中/未完结请见主文件 `doc/开发进度.md`（未完结看板）。
> 归属模块前缀：SEC-（安全）、ENC-（加密）、GIT-（Git 同步）、RIB-（Ribbon）。

| 编号 | 功能点 | 功能点详情 | 状态 | 开始时间 | 完成时间 | 记录 |
| ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| RIB-01 | Ribbon 空白区右键二级菜单 | Ribbon 空白区右键改二级菜单：一级两项（Ribbon 最大显示个数 › / 开发人员工具 ›）；ribbon 子菜单 7~15 数值（当前打勾）+ 恢复默认顺序（沿用 setMaxButtons/resetOrder）；devtools 子菜单「打开/关闭开发者工具」走 noteDesktop.toggleDevTools（IPC → main win:devtools）；外部点击关闭一级+二级（closeBlankAreaMenu 先关子菜单） | 完成 | 2026-09-17 | 2026-09-17 | npm test 172 通过（ST-37 新增 3 条 ribbon 断言），交互待手动验证 |
| GIT-01 | 初始化仓库 | Git Sync 插件：设置弹框「初始化仓库」执行 git init；git 经宿主白名单 IPC 桥接 | 开发完成 | 2026-09-17 | 2026-09-17 | npm test 159 通过，待手动交互验证 |
| GIT-09 | 入口并联 | 命令面板（新增 git-file-log/revert）+ 文件树右键（host file-tree/slot 合并插件菜单 + 注入 __pluginCtxNote/Folder）+ 左下常驻状态条三处暴露 Git 能力 | 开发完成 | 2026-09-17 | 2026-09-17 | npm test 163 通过，右键入口待手动验证 |
| GIT-12 | 历史改动文件 | 历史弹框改 --log --name-status 展示每次提交文件清单（M/A/D/R 色标），点文件看 git show <hash> -- <path> 单文件 Diff | 开发完成 | 2026-09-17 | 2026-09-17 | npm test 163 通过 |
| GIT-13 | 常驻 Git 状态指示 | 左下贴边常驻状态条遮挡目录区，取该需求；状态反馈收敛到目录树状态着色（GIT-14，监听 #file-tree 重绘→文件变动即时调色）。不再渲染状态条（删除 ensureStatusBar/showStatus/.gitsync-statusbar 样式） | 已取消 | 2026-09-17 | 2026-09-17 | 已取消，需求取消并收敛到 GIT-14 目录树着色 |
| GIT-14 | 目录树状态着色 | status --porcelain -z 解析 → 全局着色映射 window.__gsColoring（path → untracked/staged/modified）；宿主 renderFileTree 渲染 .tree-file 时直接拼 gs-* class（渲染即带状态色，无补色闪烁），已打开高亮节点不上状态色；插件状态周期/动作后更新映射并就地兜底补色；目录不再橙色（移除 gs-dirty） | 开发完成 | 2026-09-17 | 2026-09-17 | npm test 163 通过，待手动交互验证 |
| GIT-17 | 右键 Git 二级菜单 | 文件树右键笔记 → 「Git」二级菜单：对比(diff HEAD -- path 弹框)/历史(fileLogModal)/回滚(checkout -- path，confirm)/提交(add -- path+commit)/拉取(pull)/推送(push)；对比与历史弹框高度固定(gitsync-modal-lg height:76vh)。宿主 app-plugins.js contextMenus 支持 children 递归转换、file-tree-ctx.js 递归恢复 __pluginCtxNote；manifest 声明父项 Git+6 子项 | 开发完成 | 2026-09-17 | 2026-09-17 | npm test 163 通过，待手动交互验证 |
| GIT-18 | 合并提交 & 空白区 Git 菜单 | 笔记右键 Git 加「合并提交」=仅把该文件相对跟踪远端的未推送改动合并为一笔（reset mixed → add -- path → commit，其它文件改动留工作区不丢）；目录区空白处右键新增「Git」整仓二级菜单（提交/合并提交/拉取/推送），整仓合并=reset --soft upstream → commit（内容全保留仅压缩历史）；目录本身右键新增「Git」菜单（提交/合并提交/推送，针对该目录，resolveDirTarget 走 __pluginCtxFolder 可为根 ''；目录合并=reset mixed → add -- <dir> → commit 仅含该目录、目录外改动留工作区）。上游由 rev-parse @{upstream} 解析，未配置跟踪分支时提示。host main.js 白名单加 reset 并拦截 reset --hard；file-tree-ctx.js 空白区挂 file-tree-blank、目录挂 file-tree-folder slot；manifest 新增两槽（gs-ctx-repo-* / gs-ctx-dir-*） | 开发完成 | 2026-09-17 | 2026-09-17 | npm test 163 通过 + 临时目录验证整仓/单文件/目录合并语义，待手动交互验证 |