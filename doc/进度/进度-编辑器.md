# 编辑器 · 已完结进度

> 仅归档已完结条目；进行中/未完结请见主文件 `doc/开发进度.md`（未完结看板）。

| 编号 | 功能点 | 功能点详情 | 状态 | 开始时间 | 完成时间 | 记录 |
| ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| ED-01 | 文件搜索     | 占位搜索文件             | 完成 | 2026-09-02 | 2026-09-03 | jsdom端到端实测；用户实测通过 |
| ED-02 | 新建笔记     | + 按钮；右侧目录空白区右键菜单「新建笔记」也可新建（桌面版写盘） | 完成 | 2026-09-02 | 2026-09-03 | jsdom端到端实测；右键菜单新建目录/笔记写入当前目录；用户实测通过 |
| ED-08 | 多标签页            | 每文件一 Tab；**锁定/拖拽排序/溢出左右滑动/右键菜单**（锁定、关闭、关闭多个标签二级、资源管理器打开）。锁定标签固定显示在 tab 栏左端固定区、无关闭按钮、显示锁图标；标签超出显示区时在最右侧显示左移/右移按钮滑动；同区间（锁定/普通各自）拖拽重排、跨区间禁止；批量关闭一律保留锁定标签；顺序+锁定状态随增删/排序持久化到 `.second-brain/recent.json`（`{tabs,pinned}`，兼容旧纯数组），启动恢复 | 完成 | 2026-09-02 | 2026-09-05 | jsdom 启动冒烟通过；app-editor 实现 edPinned/edDirty/edDragFrom + tabDisplayOrder/renderTabs（锁/钉图标、拖拽态）/toggleTabPin/ensureTabVisible/updateTabScroll/scrollTabs/closeTabs/showTabContextMenu（含二级子菜单，app-editor-ctx 支持 children）+ 拖拽事件委托；main.js recentLoad/recentSave 适配 `{tabs,pinned}` 兼容旧数组，preload 同步注释 |
| ED-09 | 编辑/预览/分屏(所见即所得) | pencil/eye/columns 切换；编辑按钮在 源码 <-> 所见即所得(渲染可编辑) 间切换；分屏为 编辑+预览 左右并排；默认编辑模式（可被设置「默认编辑模式」覆盖） | 完成 | 2026-09-02 | 2026-09-05  | 所见即所得/预览顶部 padding 由 py-8 改为 pt-4（缩小与顶部标题/时间区间距），底部保留 pb-8；修复协调 Bug-002：窗口 resize 后补触发 renderGutter 刷新行号定位；修复协调 Bug-003：所见即所得模式右键菜单不显示 —— 右键委托扩展匹配 `#ed-wysiwyg`，按 edSource 分支新增 `buildEdWysiwygSchema` + `execWys`（execCommand + 派发 input 复用 domToMd 同步） |
| ED-11 | 文件元信息           | **标题(顶部，日期上方，默认取文件名)/日期/最后编辑/字数；编辑模式双击标题可改名同步文件名与正文** | 完成 | 2026-09-02 | 2026-09-05 | 标题双击改名：beginRenameTitle/noteTitle（同名拦截、空/未变化忽略、Esc 取消、Enter/失焦提交）；提交先保存旧路径→noteStore.move 重命名→同步 edOpenTabs/edPinned/edDirty/edNotes/edCurrent，刷新 tab/文件树/编辑区并持久化；正文首行若为 `# 旧标题` 则替换、否则前缀插入 `# 新标题`（4 用例单测通过）；新建笔记改为空正文（main.js createNote / app-note.js mock 去掉 `# 标题`），标题由标题区显示；修复协调 Bug-001：从其它视图切回编辑器时 loadView 重建 DOM 但 edCurrent 非空跳过恢复分支导致 tab/编辑区空白，在 initEditor 末尾补渲染 |
| ED-28 | 所见即所得编辑(源码可切换)  | 编辑区渲染即可点选编辑，DOM→Markdown 逆转换，源码/WYSIWYG 实时互转                                              | 完成 | 2026-09-03 | 2026-09-03  | jsdom 端到端 14 断言通过 |
| ED-29 | 分屏按钮启动/关闭分屏     | columns 点击开启分屏，再次点击关闭回编辑，高亮随布局同步                                                          | 完成 | 2026-09-03 | jsdom 14/14 | 14/14 断言全通过       |
| ED-30 | 块编辑(代码块/引用/表格/分隔线) | 所见即所得顶层块：**单击整块选中**（蓝框高亮+禁用块内光标）、**双击进入编辑**、选中态按 Enter 在后新增一行；**拖拽调整块顺序**（按落点中点判插前/后并派发 input 同步 markdown）；右键菜单补全（文本格式全套/段落设置/插入：代码块·表格·引用·任务列表·分隔线）；代码块显示**实际语言标签**（``` 后语言，`data-lang` 往返保留，语言标签 .code-lang 为 pre 兄弟、单击等同选中代码块）、渲染**不加行号**（整段无序号可贴回 markdown）；表格渲染为顶层 `<table>`（移除外层包裹 div，使单击/拖拽可命中） | 完成 | 2026-09-05 | 2026-09-05 | jsdom 往返测试 6/6 通过（代码块带/无语言标签、表格、块顺序、引用、普通文本）；app-editor 新增 edBlockSel/edDragBlock + topBlock/selectWysBlock/clearWysBlock（含 .code-lang→pre 归并）/placeCaretAtEnd + 块散列事件（单击/双击/Enter/拖拽/拖放/Esc 清除）；app-note.renderMarkdown 代码块语言标签 + data-lang、表格去掉外层 div；app-editor-ctx.buildEdWysiwygSchema 补全文本格式/段落/插入块 + insertWysBlock；css 新增 .sb-block-selected ；修复 Bug-007：所见即所得右键「插入」报错 —— insCode 代码块与「新增链接」原用 Electron 不支持的 window.prompt，改用 window.inputModal 异步输入框；「插入」二级菜单重排为 脚注→表格→标注→分割线→代码块→数学块（新增脚注 [^1] 上标、数学块 $$ $$，移除任务列表，引用改名标注）｜后续增强（2026-09-06）：所见即所得**按块类型差异化右键**——段落/标题出完整菜单（文本格式/段落设置/插入/删除本块）、列表出段落设置+插入+删除、链接出编辑/移除链接、表格出上/下插入行+左/右插入列+删除、标注(引用)出转为正文+插入+删除、代码块出设置语言+复制代码+删除、分隔线仅删除（app-editor-ctx 的 resolveWysHit 解析命中块类型→buildEdWysiwygSchema 分支生成）；**修复 Bug-016 块删除**：删除带语言标签代码块时原只删 `<pre>` 残留空 `.code-lang`、且光标被 `previousElementSibling` 取到的空语言标签吞掉乱跳——新增 `wysAdjacentContent`(跳过 .code-lang)/`placeCaretAtWysStart`(无相邻块归一到容器开头)，`deleteWysBlock` 与选中态 Backspace/Delete 统一复用，删除同删语言标签；冒烟新增用例 20b 共 **78 通过**、`npm test` 回归 5 通过。｜（2026-09-06）右键-插入与链接/表格增强：**插入二级菜单新增「链接」**（置首）+ 段落/标题选中文本时右键直接出「链接」转链；链接「编辑链接」「移除链接」保留且编辑链接改走新**双输入弹框**（链接文字+URL+确定/取消）；app-input 新增 `inputLinkModal`（返回 {text,url}），供插入/编辑链接用；**修复 Bug-017 块后插入**：`insertWysBlock(html,anchor)` 新增 anchor（取 `edCtxHit.block`），块插入到右键命中块之后而非首行；**表格默认插入 3×3**（3 表头 th + 2 数据行共 6 td），插入后光标落首个 td；**末尾块补空段**：WYSIWYG 渲染 `appendWysTrailingP` 在末尾块(PRE/TABLE/BLOCKQUOTE/HR)后补空 `<p><br></p>` 便于回车加行且 domToMd 剔除不留源空行，段落结尾不重复补；冒烟新增用例 22/23/24 共 **86 通过**、`npm test` 回归 5 通过｜（2026-09-07）**编辑区高度占满**：源码 textarea `autoResizeTa` 由「高度=scrollHeight」改为「高度=max(scrollHeight, 剩余可视高)」——内容不足时编辑区撑满至与行号 gutter 通栏等高（占满 `#ed-split` 可视区），内容超出时仍随内容增长；jsdom 场景 A(内容60→撑满500)/场景 B(内容900→随内容900) 均通过，`node --check` 通过 |
| ED-18 | 面板头部         | 侧边面板+展开全部+关闭                                                                                                                                                                                                                    | 测试完成 | 2026-09-02 | 2026-09-03 | 右侧边栏可收起/展开（边缘把手）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ED-19 | 大纲           | 标题结构列表                                                                                                                                                                                                                          | 测试完成 | 2026-09-02 | 2026-09-03 | jsdom端到端实测                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ED-20 | 反向链接         | 引用卡片+预览+计数                                                                                                                                                                                                                      | 测试完成 | 2026-09-02 | 2026-09-03 | jsdom端到端实测                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ED-22 | 文件树空白区右键     | 空白处右键 → 新建笔记/新建目录，空目录即时显示                                                                                                                                                                                                       | 测试完成 | 2026-09-03 | 2026-09-03 | jsdom 端到端 10 断言通过                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ED-23 | 左侧栏收起        | 文件树头部收起按钮+边缘把手展开                                                                                                                                                                                                                | 测试完成 | 2026-09-03 | 2026-09-03 | jsdom 端到端 13 断言通过                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ED-24 | 工具栏右对齐+滚动条统一 | 工具栏移右上、新增分屏按钮、全局滚动条统一                                                                                                                                                                                                           | 测试完成 | 2026-09-03 | 2026-09-03 | jsdom 端到端实测（ED-09 分屏并入）；仅最右侧一条滚动条                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ED-25 | 隐藏文件+工具栏文件名  | 默认隐藏 . 开头目录/文件，右键菜单开关；工具条左侧显示当前文件名                                                                                                                                                                                              | 测试完成 | 2026-09-03 | 2026-09-03 | jsdom 端到端实测                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ED-26 | 标签栏一体化       | 移除独立工具条横栏，编辑/预览/分屏/删除按钮合并进标签栏行右侧                                                                                                                                                                                                | 测试完成 | 2026-09-03 | 2026-09-03 | jsdom 端到端实测（42 断言全绿）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ED-27 | 编辑区右键菜单      | 仅打开 .md 时编辑区右键：链接/文本格式/段落设置/插入/剪贴板，含二级子菜单与格式应用                                                                                                                                                                                  | 测试完成 | 2026-09-03 | 2026-09-03 | jsdom 端到端 16 断言通过                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ED-32 | 文件属性（右侧面板）   | 侧边面板首个区块展示：文档 id（frontmatter id，无则回退路径）、相对路径、所在目录笔记个数与占用大小、每篇笔记属性（大小/字数/标签/更新时间）、索引分块起止规则（块数 / 每块约 200 字符 / 相邻重叠 40 / 每块字符区间 start–end）                                                                                         | 测试完成 | 2026-09-05 | 2026-09-05 | 根因修复：`$('ed-fileprops')` 误带 `#` 导致 getElementById 返回 null→空白；改为元数据驱动（见 ED-33），AI listIndex 仅作索引分块数据源；chunkTextDetailed 返回每块 start/end 并在 rebuildIndex/updateNote 写入、load/save/listIndex 透传；旧索引无区间显示 `–`；node --check 通过（2026-09-05）                                                                                                                                                                                                                                                                                                  |
| ED-33 | 元数据目录        | 知识库内隐藏 `.second-brain/` 目录：每目录一条 `_meta.json` 记录（dir/笔记个数 noteCount+totalNotes/占用大小 size/每篇 id·字数·标签·大小·mtime）；新建/保存/删除/移动目录或笔记、切换库时 scheduleMetaRefresh 同步重建并 pruneMeta 清理残留镜像；notes:fileMeta 缺失时自动重建；ai-engine.\_scanMd 排除该目录 | 测试完成 | 2026-09-05 | 2026-09-05 | main.js 新增 scanVaultMeta/writeVaultMeta/pruneMeta/parseNoteMeta + notes:refreshMeta、notes:fileMeta IPC；preload 暴露 refreshMeta/fileMeta；6 个变更 handler + vault:choose/switch 挂 scheduleMetaRefresh；端到端测试：根/多级/空目录记录文件生成、id/标签/个数/大小正确、删除目录清理镜像、移动目录迁移镜像 —— 全部通过（2026-09-05）                                                                                                                                                                                                                                                            |
| ED-35 | 目录属性/重建      | 目录具备属性：children 文件列表(子目录+文件，各带 size/mtime)+占用大小；文件/目录增删改移动逐级向上同步到各级父目录属性；左侧选中目录显示目录属性、笔记面板保留「所在目录内容」；「重建元数据」按钮+打开/切换库自动构建                                                                                                       | 测试完成 | 2026-09-05 | 2026-09-05 | main.js scanVaultMeta 增加每目录 children(文件+子目录各带 size/mtime、目录在前按中文排序)、size 自底向上聚合；新增 notes:dirMeta IPC；preload 暴露 dirMeta；editor.html 文件属性头部加重建按钮(refresh-cw)；app-editor 新增 edSel 选中目录状态、重写 renderFileProps 双视图(目录属性/笔记属性)、移除原「所在目录」汇总行改「所在目录内容」、选中目录与重建按钮联动；vault:get 启动自动构建；端到端测试：children 排序/逐级聚合(a.totalSize==sum(children))、新增笔记向上级联( b→a→root 全变)、删除目录清理镜像并去除父 children —— 通过（2026-09-05）。后续调整：目录属性视图移除「文件列表」，新增「创建时间/修改时间」（dirMeta 对目录实体 stat birthtimeMs/mtimeMs 附加 created/mtime，根目录→库根），并删除未再使用的 childrenList 函数（2026-09-05） |
| ED-34 | 雪花文档 id      | 每个笔记拥有唯一雪花 id 恒非空；**不再写入 md frontmatter（md 保持干净）**，id 由 `.second-brain/_meta.json` 的 `noteId` 追踪：首次元数据扫描生成一次、之后复用；历史 md 中已写入的 `id:` 字段在扫描时自动清理                                                                                                                            | 测试完成 | 2026-09-05 | 2026-09-05 | 安装成熟库 snowflake-id ^1.1.0（prod                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ED-37 | 最近打开恢复 | 启动时读取 `.second-brain/recent.json` 上次打开的多个标签页并逐一恢复；打开/关闭/删除标签时同步持久化列表 | 测试完成 | 2026-09-05 | 2026-09-05 | main.js 新增 notes:recentLoad/recentSave IPC（读写 .second-brain/recent.json，recentLoad 过滤已删除项）；preload 暴露 recentLoad/recentSave；app-editor 新增 persistRecentTabs（openNote/关闭/删除标签后调用）+ 启动恢复优先读最近列表（图谱跳转目标优先，再 fallback 打开第一篇）；同时修复 walkNotes 未跳过 .second-brain 导致启动误开内部目录的 EISDIR 问题；防启动逐个装载闪烁：恢复改为仅建多标签、只读取激活「最后打开的文件」其余懒加载，persistRecentTabs 条目打开/关闭/删除时同步且把当前激活移到列表末尾；结论：多标签持久化与懒加载恢复链路验证通过（2026-09-05） |
| ED-03 | 文件夹展开/折叠 | 左侧文件树文件夹点击展开/折叠子项，图标随状态联动 | 完成 | 2026-09-05 | 2026-09-07 | 用户实测通过 |
| ED-04 | 文件夹计数徽标 | 文件夹行显示其下笔记数量徽标 | 完成 | 2026-09-05 | 2026-09-07 | 用户实测通过 |
| ED-06 | 文件选中态 | 点击文件行高亮品牌色选中态 | 完成 | 2026-09-05 | 2026-09-07 | 用户实测通过 |
| ED-05 | 文件行与相对日期 | 文件树每行：`file-text` 图标 + 文件名 + 相对日期徽标（今天 / 昨天 / N天前）；点击行选中并打开该笔记；选中态品牌色底 + 白字 | 完成 | 2026-09-05 | 2026-09-07 | relDate 相对日期（今天/昨天/N天前）；用户实测通过 |
| ED-07 | 库统计 | 库整体统计：共 N 篇笔记 · 大小（MB） | 完成 | 2026-09-05 | 2026-09-07 | 数据来自元数据 _meta.json 聚合；用户实测通过 |
| ED-23 | markdown-editor 插件按功能拆分（段/表/列表/任务/代码/链接/标注/脚注/数学，源码+所见即所得双实现） | 每个功能独立 js，manifest.scripts 拼接同闭包 | 完成 | 2026-09-04 | 2026-09-07 | npm test 5 + 冒烟 86；用户实测通过 |
| ED-25 | WYSIWYG 块编辑增强 | 代码块语言选择/块后插入/表格3×3光标落td/末尾块补空行；表格空单元格 `<br>` 占位+td最小高度、切模式补末尾空段、单击不入整块可逐格编辑、块后空段一次退格删块、`#ed-wysiwyg table` CSS 兜底格线；表格右键行/列增删按命中单元格所在行列定位，新增「删除该行/删除该列」（删行保护表头+1数据行、删列每行至少留1列） | 完成 | 2026-09-04 | 2026-09-07 | Bug-019~023、025，npm test 25 通过；用户实测通过 |
| ED-26 | markdown-editor 插件落地渲染公共层（md-render） | esc/inline/renderMarkdown 迁入 `plugins/markdown-editor/common/md-render.js`（manifest.scripts 首项）；代码块语言仅存 `<pre data-lang>`，去除可见 `.code-lang` 幽灵标签（宿主 app-note 回退与 applyCodeLang 同步） | 完成 | 2026-09-04 | 2026-09-07 | Bug-018；npm test 7 通过；用户实测通过 |
| ED-39 | 表格右键删除行列 | 右键菜单新增「删除该行/删除该列」，宿主+插件 `wysTableOp` 支持 `row-delete`/`col-delete`；删行保护表头+1数据行、删列每行至少留1列 | 完成 | 2026-09-06 | 2026-09-07 | npm test 25 通过（Bug-Col 断言扩展）；用户实测通过 |
| ED-14 | 所见即所得代码块悬浮复制（ED-14 扩展） | 所见即所得模式代码块悬停右上角显示复制按钮（body 级单例浮层 #wys-copy-btn），悬停显示/移出隐藏/移到按钮保持，点击复用 wysCopyBlock 复制并切对勾反馈；`bindWysCopyButton` 独立可测，浮层不污染 `<pre>` 可编辑结构、markdown 往返干净 | 完成 | 2026-09-07 | 2026-09-07 | npm test 33 通过（Bug-026 显隐/锚定/点击反馈断言） |
| ED-40 | 选中文本悬浮工具栏 | 选中普通文本/列表项/表格内文本时，选中内容上方弹出悬浮工具栏，提供字体/字号/颜色/背景色/加粗/斜体/下划线/删除线/段落/数学($行内包裹)/清除格式；与右键格式菜单**共用同一套格式动作** `wysTextFmtGroup`/`wysParaFmtGroup`（js/editor/editor-format.js 共享宿主+插件）；数学 `$…$` 渲染为原子内联块 `.sb-math`（contenteditable=false），点击整体高亮、退格整体删除（editor-host.js wys keydown 分支）；字体/字号/颜色/背景色经 HTML 透传持久化到 markdown（保存 `<font>/<span style>/<u>`，`inline()`/`inlineToMd()` 白名单还原与 outerHTML 序列化，往返稳定）；悬浮工具栏由 body 级单例 `#wys-sel-tool`（editor-wys-toolbar.js）承载，选区上方居中、顶缘翻转、左右 clamp，Escape/点击外部/切源码隐藏 | 测试完成 | 2026-09-07 | 2026-09-07 | npm test 48 通过（ED-40 断言：`$…$` 往返、`<font>` 透传稳定、数学包裹/解包、工具栏单例与显隐控制） |
| ED-41 | 选中文本右键格式菜单 | 右键菜单提供与悬浮工具栏相同格式设置项（字体/字号/颜色/背景色/斜体/加粗/下划线/删除线/段落/数学），与原生右键菜单合并；宿主 `js/app-editor-ctx.js` 与插件 `plugins/markdown-editor/features/context.js` 的 `buildEdWysiwygSchema` 同步替换 `textFmt`/`paraFmt` 为共享 `wysTextFmtGroup()`/`wysParaFmtGroup()`，两种触发形式底层一致 | 测试完成 | 2026-09-07 | 2026-09-07 | npm test 48 通过；与 ED-40 共用格式核心，jsdom 已验证渲染/序列化与共享函数注入 |
| ED-43 | 自研编辑增强退役 | 弃用并退役自研 Markdown 编辑增强：**ED-40 选中文本悬浮工具栏**、**ED-41 右键格式菜单**、HTML 透传（`<font>/<span style>/<u>`）、数学块（`$…$`）、代码块悬浮复制、块编辑/表格行列右键（原 ED-39）、宿主查找替换条（原 ED-33/ED-34 的 Host 版）——全部由 vditor 原生能力承接 | 完成 | 2026-09-07 | 2026-09-07 | ED-40/41 及 HTML 透传、$…$ 数学块、代码块悬浮复制、块编辑、表格行列右键、宿主查找替换条全部退役，由 vditor 原生承接；插件 `common/features/styles` 删除（已备份）；index.html 移除 editor-format/editor-wys-toolbar/editor-ctx 引用；host findbar DOM/快捷键下线 |
| ED-42 | vditor 引擎接入 | 引入 vditor（`js/vendor/vditor`）作为 `.md/.markdown` 编辑区唯一实现；单 `#ed-vditor` 容器全替换退役的 `#ed-edit`/`#ed-wysiwyg`/`#ed-preview` 三套 DOM；`.md` Provider 静态注册进宿主，桌面与网页共用；行号/自动换行/查找替换/代码高亮/数学/表格/导出等由 vditor 原生承接；编辑区高度撑满（#ed-vditor → .vditor → .vditor-content 全链 flex）；三个模式按钮各自绑定 vditor 能力：**编辑** = 仅编辑区，节点在 WYSIWYG/IR 间择一（点「编辑」弹冒泡子菜单二选一，默认 IR）；**预览** = 纯预览只读；**分屏** = SV 源码分屏；编辑态被预览/分屏打断后返回自动恢复上次所选编辑节点 | 完成 | 2026-09-07 | 2026-09-08 | 桌面/网页双端沿用；用户实测通过｜（2026-09-09）修复 Bug-031：`js/vendor/vditor/` 从 npm 包平铺结构重排为官方 CDN 目录结构——将 `content-theme/css/images/js/ts/types` 与 `index.css/index.js/index.min.js/method.*` 等整体移入新建 `dist/` 包裹层（否则 vditor 按 `{cdn}/dist/js/...` 拼出 `/dist/js/i18n/zh_CN.js` 等全部 404）；同步 `index.html` 样式/入口引用为 `dist/index.css`、`dist/index.min.js`，`cdn: 'js/vendor/vditor'` 保持不变；本地 http.server 实测关键资源均 200，`npm test` 回归 **38 通过、0 失败** |
| ED-12 | Markdown 渲染 | H1/H2、段落、列表、待办、行内代码/加粗/链接由 vditor 渲染承接 | 完成 | 2026-09-02 | 2026-09-08 | vditor 原生渲染；用户实测通过 |
| ED-13 | 待办勾选 | 方框/对勾/删除线由 vditor 承接 | 完成 | 2026-09-02 | 2026-09-08 | vditor 原生勾选；用户实测通过 |
| ED-14 | 代码块 | `terminal` + 语言标 + 复制按钮 + 语法染色由 vditor 承接 | 完成 | 2026-09-02 | 2026-09-08 | vditor 原生高亮/复制；用户实测通过 |
| ED-15 | 引用块 | 左侧竖线 + 斜体由 vditor 承接 | 完成 | 2026-09-02 | 2026-09-08 | vditor 原生渲染；用户实测通过 |
| ED-16 | 文章标签区 | 正文底部标签 chips 展示 | 完成 | 2026-09-02 | 2026-09-08 | 呈现完成；用户实测通过 |
| ED-17 | 编辑器状态栏 | 底部条：已自动保存 + 语言(Markdown) + 编码(UTF-8) + 行/列 + 字数 + 当前语言 | 完成 | 2026-09-02 | 2026-09-08 | 呈现完成；用户实测通过 |
| ED-44 | 文件树重命名 | 文件树笔记行改名：右键菜单「重命名」/双击文件名（点击内时间窗判定）就地替换输入框，Enter/失焦提交、Esc 取消；提交 `noteStore.move` 重命名 `.md`（保留内容与 noteId，未保存先落盘再移动），同步刷新 tab/文件树/编辑区/最近打开并持久化；同名冲突拦截；编辑器顶部标题/时间/字数组件移除，vditor 直通顶部 | 完成 | 2026-09-07 | 2026-09-08 | npm test 22 通过；用户实测通过 |
| ED-45 | 统一编辑模式记忆 | 收敛「编辑/预览/分屏」为统一编辑模式 WYSIWYG/IR/SV/阅读；点「编辑」切回上次编辑节点（WYSIWYG/IR）、悬浮弹 4 项模式菜单选择并记忆；预览/分屏独立按钮保留联动；模式持久化 `sbEditorMode`（默认 ir）；目录区打开文件与启动/托盘回主窗口按上次记忆模式恢复；**分屏按钮**点击在 `分屏(both 源码+预览)→仅源码(editor)→仅预览(preview)` 间循环切换（三布局均基于 SV，仅切 vditor `preview.mode`）、悬浮弹布局菜单三选一直选；布局持久化 `sbSplitLayout`（默认 both）；**预览按钮**点击切到 SV 纯预览（`preview.mode=preview`，隐藏源码）。修复：`vdSetMode('edit')` 不再调用 vditor `setPreviewMode('editor')`（其强制 sv 源码 textarea display:block，导致切换页签/新开笔记时源码区 50% 挤压编辑区）；新增 `applyVdVisibility` 确定性接管 sv/ir/wysiwyg/preview 显隐，构建期写入 `preview.mode` 并在 `after` 回调二次强制（修复重建/异步时序下仅源码/仅预览布局残留）。（移除原分屏开关式及 `modeBeforeSplit` 变量，改由 SV+`preview.mode` 承载）。另按「设置-外观-主题模式」统一 vditor 主题：构建期读 `html[data-theme]` 解析结果设 vditor `theme`，新增 `window.vdSyncTheme`+`MutationObserver(data-theme)` 在运行中切换主题即调用 `setTheme` 即时同步 `.vditor--dark`。修复深色下编辑内容不可见：该版本 vditor 的 `.vditor-reset` 硬编码 `color:#24292e`（浅色文字），深色背景同色致内容不可见，在 app.css 加 `#ed-vditor.vditor--dark .vditor-reset{color:var(--note-ink)}`（`.vditor--dark` 加在容器自身，用组合选择器） | 完成 | 2026-09-07 | 2026-09-08 | npm test 通过；CDP 实机验证通过（循环切换/预览/跨笔记变窄回归/主题同步+深色可见性均达标） |
| ED-48 | 图片/附件上传 | Markdown Editor 编辑器支持上传「图片+附件（pdf、html、sh、bat、zip、doc、excel 等）」：vditor 工具栏新增 **upload** 按钮，`accept` 覆盖 `image/*` 与 `.pdf/.doc/.docx/.xls/.xlsx/.ppt/.pptx/.html/.htm/.md/.txt/.sh/.bat/.cmd/.ps1/.csv/.zip/.rar/.7z/.asc`，支持多选、单文件 ≤50MB；上传文件**落盘到知识库根级隐藏资源目录 `.resources`**（`.` 开头，walkNotes/scanVaultMeta 跳过，文件树不可见），磁盘 **UUID 重命名保留扩展名**，笔记内链接**使用真实文件名**（图片 `![真实名](note://vault_res/uuid.ext)`、附件 `[真实名](note://vault_res/uuid.ext)`）；`note://` 协议新增 `vault_res` 主机路由 → `.resources`；附件链接点击经捕获阶段拦截，调 `notes:openResource` 用系统默认程序打开，避免窗口整体导航 | 测试完成 | 2026-09-11 | 2026-09-11 | main.js 新增 RESOURCE_DIR(.resources)+note 协议 `vault_res` 路由+`notes:uploadResource`(UUID 命名保留扩展名)/`notes:openResource`(防路径穿越) IPC+walkNotes/scanVaultMeta 排除 `.resources`；preload 暴露 uploadResource/openResource；editor-vditor.js 新增 upload 按钮与 `upload.handler`(读二进制→IPC 落盘→按真实名插入)、`window.sbResMarkdown/resolveVaultResUrl` 助手、附件点击拦截；集成为桌面端；`npm test` 回归 **117 通过、0 失败**（新增 8 条 resources 断言）；node --check 3 文件通过；功能逻辑(落盘/隐藏/扩展名/防穿越)临时脚本验证通过；**网页版暂不支持**（无桌面桥接时提示「上传仅桌面版支持」，待配 web 上传服务）。交互增强：**拖拽到指定位置上传**（vditor 原生 drop→`upload.handler`，光标处插入）；**编辑区右键「上传并插入到此处…」**（`contextmenu` 拦截 + `caretRangeFromPoint` 定位光标 + 惰性文件选择框）；新增 **Obsidian 附件语法 `> [!attach] 真实名 note://vault_res/uuid.ext`**：预览经 vditor `preview.transform`（`window.sbTransformPreviewHtml`）→ 附件卡片（`window.sbAttachCardHTML`，扩展名徽标+真实名+下载按钮+data 属性），编辑区经 `markAttachCallouts`+MutationObserver 加 `sb-attach-callout` 指示类（防抖、仅切 class 不改内容）；卡片点开（系统默认程序）/右键「下载(`sbDownloadResource` fetch→blob→下载)/复制链接」；右键菜单 `window.sbShowMenu/sbHideMenu`（防溢出、Esc/外部点击关闭）；app.css 增 `.sb-res-card/.sb-res-card-ext/.sb-res-card-dl/.sb-ctx/.sb-ctx-item/.sb-attach-callout` 样式；资源助手统一导出 `window.__vdRes`。`npm test` 回归 **124 通过、0 失败**（新增 7 条 card 断言，累计 15 条资源断言）；node --check 通过。<br>**上传附件渲染为卡片修正（2026-09-12）**：上传的**非图片附件**不再插入普通链接 `[名](url)`，改为插入 **`> [!attach] 名 url` 卡片引言**（新增 `window.sbAttachMarkdown`），使上传附件在编辑区直接呈现「醒目指示块」、预览/阅读渲染为「完整卡片」；`markAttachCallouts`/`transformPreviewHtml` 选择器扩为 `blockquote, [data-type="blockquote"]` 并容错 <p> 缺失；`npm test` 回归 **130 通过、0 失败**（新增 SV data-type 指示 + 5 条 upload 分流断言）。<br>**右键菜单增强（2026-09-12）**：①IR 表格/代码块右键「在表格上方插入空行/在代码块上方插入空行」统一改名为 **「在上方插入空行」**；②编辑区通用右键菜单由单项「上传并插入到此处…」扩展为 **「插入图片」（accept `image/*`，插行内 `![名](url)`）与「上传附件」（accept 非图片白名单，插 `> [!attach]` 卡片引言）**，新增 `window.sbUploadAccept(type)` 解析 accept；③避免与 IR 表格/代码块专属菜单叠加（IR 命中 `table/pre` 时通用菜单放行）；`npm test` 回归 **135 通过、0 失败** |
| ED-49 | Markdown Editor 插件增强化 | 将 `markdown-editor` 插件从「声明外壳」重构为**编辑器的真正增强**，把 markdown 专属实现代码放回插件内：`plugins/markdown-editor/` 新增 **`md-serialize.js`**（往返序列化 `inlineToMd`/`domToMd`）、**`md-blocks.js`**（代码块语言选择器 + WYSIWYG 块编辑：语言 chip/下拉搜索/应用、块选中/清空/光标归位/末尾补空段）、**`md-context.js`**（右键插入与上传：`insWysPickUpload`/`handleWysPickFiles`/`wysUploadBlockHtml`/`wysEsc`/`sbUploadAccept`/`irInsertAbove`），配套样式收归插件 `styles.css`；宿主 `editor-md.js`/`editor-vditor.js`/`app-editor-ctx.js` 收口为薄壳委托（经 `window.sbMdBridge.*`，取不到回落宿主兜底）；`main.js` 先注册编辑器 Provider（编辑/预览/分屏、右侧边面板）再注册命令，`manifest.json` 声明 `scripts`/`styles` 并在桌面版目录插件加载时覆盖内置描述；网页版（不加载目录插件）回落宿主薄壳 | 完成 | 2026-09-12 | 2026-09-12 | 桌面版目录插件加载后以插件 Provider + 增强代码承接编辑能力（往返序列化/语言选择器/块编辑/右键插入·上传）；网页版回落宿主薄壳兜底。冒烟脚本 `smoke-plugin.test.js` 增 `PLUGIN_SCRIPTS`（注入插件模块）与多种子断言；`npm test` 回归 **137 通过、0 失败**；`node --check` 新增插件模块通过 |
