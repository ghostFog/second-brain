# 插件框架/规范 · 已完结进度

> 本文件仅归档**插件框架与平台规范**功能（插件市场、目录机制、Ribbon/右键/命令面板、编辑器能力、快捷键、版本同步等）。
> **单个插件**的功能各自归档到独立文件（见 `doc/开发进度.md` 的「已完结历史（插件）」）。
> 仅归档已完结条目；进行中/未完结请见主文件 `doc/开发进度.md`（未完结看板）。

| 编号 | 功能点 | 功能点详情 | 状态 | 开始时间 | 完成时间 | 记录 |
| ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| PL-02 | 管理已安装插件 | package-check 按钮 | 测试完成 | 2026-09-02 | 2026-09-03 | jsdom端到端实测 |
| PL-03 | 搜索插件    | 按名称/描述过滤卡片       | 测试完成 | 2026-09-02 | 2026-09-03 | jsdom端到端实测 |
| PL-04 | 排序      | 热门/最新/评分最高       | 测试完成 | 2026-09-02 | 2026-09-03 | jsdom端到端实测 |
| PL-05 | 分类过滤    | 分类 Chip          | 测试完成 | 2026-09-02 | 2026-09-03 | jsdom端到端实测 |
| PL-08 | 安装/卸载 | 安装/已安装状态切换   | 测试完成 | 2026-09-02 | 2026-09-03 | jsdom端到端实测 |
| PL-09 | 详情抽屉       | 右侧滑出详情面板                                                | 测试完成 | 2026-09-02 | 2026-09-03 | jsdom端到端实测                                                                                                                                                                                                                                                                     |
| PL-10 | 插件左侧图标注册   | 已安装插件可向 Ribbon 注册自定义图标入口                                | 测试完成 | 2026-09-04 | 2026-09-04 | PluginManager 核心完成：materializePlugin 把 JSON 声明性 manifest（ribbon/commands/contextMenus）注入 actionKey 实际回调；refreshPluginRibbonButtons 在 spacer 前动态插入 data-plugin-ribbon 按钮；Calendar/Minimal Theme 预设 ribbon 并随启动自动注册；vm 集成测试 10 断言通过                                              |
| PL-11 | 插件右键菜单注入   | 插件可向文件树/编辑区注入自定义右键菜单项                                   | 测试完成 | 2026-09-04 | 2026-09-04 | file-tree-ctx.js 的 showFileTreeContextMenu 追加 pluginManager.getContextMenus(isFolder? 'file-tree-folder':'file-tree')；app-editor-ctx.js 的 buildEdCtxSchema 追加 pluginManager.getContextMenus('editor')；有菜单项时前置 '-' 分隔线；Calendar 预设 file-tree 菜单、Git Sync 预设 file-tree+editor 菜单 |
| PL-12 | 插件被命令面板搜索到 | 插件命令自动注册到 Ctrl+P 命令面板                                   | 测试完成 | 2026-09-04 | 2026-09-04 | app-core.js 的 buildCommandList() 追加 pluginManager.getCommands() 分组，每组标题为「插件名 插件」；命令面板过滤逻辑自然覆盖插件分组；Calendar 预设 1 条命令、Git Sync 预设 3 条命令，安装后 Ctrl+P 即可搜到                                                                                                                          |
| PL-13 | 插件目录机制     | 插件为独立目录 plugins/<id>/（manifest.json + main.js），启动自动扫描加载 | 测试完成 | 2026-09-05 | 2026-09-05 | main.js 新增 scanPlugins()/plugins:list/plugins:reveal IPC；preload 暴露 noteDesktop.plugins；app-plugins.js 新增 PluginAPI 与 loadDirPlugins()（fetch main.js 注入沙箱执行 + manifest 合并目录优先）；内容高亮插件迁移为 plugins/highlight/ 目录插件；package.json 打包加入 plugins/\*\*；渲染进程 19 断言 + 主进程扫描 10 断言全过     |
| PL-14 | 编辑器能力插件化（md 操作独立） | 新增 EditorProvider 抽象：插件注册后缀/打开方式/工具按钮/侧边面板，核心按后缀路由到 Provider；md 操作搬入 plugins/markdown-editor/ 目录插件（承接源码/所见即所得、预览、索引面板操作）；无匹配后缀走内置纯文本兜底 | 测试完成 | 2026-09-06 | 2026-09-06 | app-plugins.js 新增 editorProviders/getEditorProviders(ext)/getEditorExtensions()/fileExtension()/fallbackEditorProvider + PluginAPI.editor 桥(getState/renderMarkdown/setMode/readNote/saveNote)与 PluginAPI.registerEditorProvider；plugins/markdown-editor/{manifest,main,styles} 注册 .md/.markdown + openers(edit/preview/split) + toolbar/sidebar；app-editor.js openNote(path,{mode}) 按后缀取 Provider、applyOpenMode 应用默认打开方式（defaultOpenMode 读 openAs:<ext>，缺省 edit）；设置-编辑器新增「文件类型」映射表 fileTypesHtml()（遍历 getEditorExtensions，每后缀 select data-skey="openAs:<ext>"，openers 来自命中 Provider）；文件树笔记右键首项改为「打开」二级子菜单 buildOpenChildren()（在资源管理器打开 + 各 Provider openers → openNote(path,{mode:openerId})）；smoke-plugin.test.js 扩展 8 项断言（Provider 路由/兜底/后缀收集/文件类型表/打开子菜单），23/23 通过；node --check 全过     |
| PL-15 | 插件排序 | 内置 Markdown Editor 置为插件列表首位（市场第一张卡片 + 设置已安装列表第一项） | 测试完成 | 2026-09-06 | 2026-09-06 | DEFAULT_PLUGIN_DATA 首位插入 Markdown Editor（installed=false、downloads=22000 使市场按下载量默认排序置顶；桌面版由 loadDirPlugins 以同 id 目录插件 Object.assign 合并，保留首位并置 installed=true）；smoke 新增「市场第一张卡片为 Markdown Editor」断言，24/24 通过     |
| PL-16 | 插件命令注册快捷键 | 插件命令可声明默认快捷键并在设置-快捷键重绑；内置+插件命令统一由注册表管理、冲突检测 | 测试完成 | 2026-09-06 | 2026-09-06 | 新增 js/app-keybinds.js 快捷键注册表（kbRegisterCommand/kbMatch/kbSetBind/kbClearBind/kbResetAll/kbUnregisterPlugin）；app-plugins.js 的 install 将插件命令注册进注册表（启用时、默认键仅在无冲突时占用）；app-layout.js 全局 keydown 改走 kbMatch（保留命令面板 UI 键）；app-settings.js「快捷键」改为注册表驱动动态渲染 + 设置/清除捕获绑定 + 恢复默认；app-core.js 命令面板快捷键取 kbResolveBind（用户绑定>默认）；index.html 引入 app-keybinds.js；新增 smoke-keybind.test.js 15/15 通过，smoke-plugin 24/24、回归 5/5 通过 |
| PL-19 | 插件版本号与配置同步 | 插件 manifest 支持版本号；设置-插件管理中展示版本；检测到新解压插件或版本号变化时同步补齐设置默认值 | 测试完成 | 2026-09-06 | 2026-09-06 | 目录插件 manifest.json 的 version 字段即版本号；DEFAULT_PLUGIN_DATA 为 markdown-editor/minimal-theme/code-highlight 三个目录插件 mock 补 version；app-plugins.js 新增 syncPluginConfig()（loadDirPlugins 安装后调用：首次发现 isNew 或版本号变化 changed 时，为 settings schema 中未保存项写入默认值 localstorage plugin:<id>:<key>，不覆盖既有配置，并记录 plugin:<id>:__version__ 便于下次比对），globalThis.syncPluginConfig 导出；app-settings.js 的 pluginMgrItemHTML 增补版本徽标（行内 v1.0.0）与「基本信息-插件版本」行；smoke-plugin.test.js 新增 6 项版本断言（徽标/版本行/新插默认/不覆盖/升级补默认），39/39 通过 |
| PL-20 | 插件市场展示版本号 | 插件市场卡片与详情抽屉展示插件版本号 | 测试完成 | 2026-09-06 | 2026-09-06 | renderPlugins 卡片标题行 + openPluginDrawer 详情头部，标题后追加「vX.Y.Z」版本徽标（font-mono 小号标签，无 version 不显示）；DEFAULT_PLUGIN_DATA 补齐全部 mock 插件 version:1.0.0（calendar/kanban/excalidraw/git-sync/mind-map/notion-sync/dark-mode-pro，加已含的 markdown-editor/minimal-theme/code-highlight）；smoke-plugin.test.js 新增「市场卡片展示插件版本徽标」断言，40/40 通过 |
| PL-01 | 页标题 | 插件市场页标题 + 副标题 | 完成 | 2026-09-02 | 2026-09-07 | 用户实测通过 |
| PL-06 | 卡片信息 | 插件市场卡片：图标块、名称、作者、两行描述 | 完成 | 2026-09-02 | 2026-09-07 | 用户实测通过 |
> 单个插件功能归档（按插件）：
> - Markdown Editor → `进度-插件-MarkdownEditor.md`
> - Minimal Theme → `进度-插件-MinimalTheme.md`（PL-18、PL-21）
> - Code Highlight → `进度-插件-CodeHighlight.md`（PL-17）

> 历史插件（已不在 `plugins/`，仅保留记录）：内容高亮（HIGHLIGHT-01）——多颜色 Markdown 高亮，曾作为首个目录插件原型，后移除。
