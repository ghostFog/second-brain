# 工程与运行记录

> 拆分时未归入具体模块的历史正文/附加记录（含日志落盘、运行期排查、超长/转义的历史表行等）。



***




***




***




```
       依赖，已入 package.json）；main.js 新增 `snowflake = new SnowflakeId({mid:1})` 单例 + `ensureNoteSnowflakeId`，scanVault
       Meta 逐篇 ensure + 变更才写回；测试：有 id 保留 / 无 frontmatter 注入 / 有 frontmatter 无 id 补行 / 全部唯一 / re-scan 不再变更（幂等）/ 记录 n
       oteId 无空 —— 通过（2026-09-05）  变更（2026-09-05）：不再写入 md frontmatter，id 改由 .second-brain 元数据 noteId 追踪 —— main.js 用 `ensureNoteId`（复用前次元数据 id → 既有 frontmatter id 迁移 → 否则新生成，均不写 md）+ `stripMetaId`（按行剔除 frontmatter `id:` 行；仅 id 时整块删空保留正文）在 scanVaultMeta 清理历史 id；ai-engine 新增 `_metaNoteIdFor`（优先读元数据 noteId，回退 frontmatter id/路径）供 rebuildIndex/updateNote 建立索引 noteId；测试：id 在中间保留其它字段、仅 id 整块删除、无 id/无 frontmatter 不动 —— 通过  |
```

\| ED-38 | 索引分块配置 | 全局默认「块大小/相邻重叠/单块长度上限(max\_chunk\_size)」在 设置→AI问答→知识库索引 配置，max\_chunk\_size 全局统一默认 300（0=不限制，生成宽度取 blockSize 与该值的较小者），笔记不单独设置、直接用统一设置；块大小/重叠可在侧边属性-索引分块按单篇覆盖；块列表两列（块名称 b0/b1… + 偏移值），支持编辑偏移值并分两种模式：仅本块（只改当前块，其他不变）/ 整体平移（当前块变、后续整体平移相同差值）；保存/恢复默认持久化到笔记 chunk 覆盖（\_meta.json），按默认推导还原步长切分 | 测试完成 | 2026-09-05 | 2026-09-05 | ai-engine 新增 DEFAULT\_CONFIG.blockSize/overlap/maxChunkSize + chunkConfigured(支持显式 offsets 预览 + maxChunkSize 封顶)，rebuildIndex/updateNote 经 \_chunkParamsFor 读单文件 chunk 覆盖否则全局默认；main.js 新增 notes:saveNoteChunk(save/reset覆盖，覆盖不含 maxChunkSize（全局统一）) + ai:previewChunk(读盘切分)；preload 暴露 saveNoteChunk/previewChunk(+maxChunkSize)；app-settings 知识库索引新增块大小/重叠/单块上限输入(data-ai-cfg 自动收集保存，缺省 300)；app-editor renderChunkSection 可编辑面板（仅本块/整体平移/按默认推导/保存/恢复默认，单块上限不作单文件输入、用全局值预览）；node --check 全部通过；chunkConfigured 逻辑测试：默认300+块大小200→宽仍 200/200/200/120（块大小优先）、上限150→150/150/150/120、stride 仍 \[0,160,320,480]、修复 max(1,cap) 使默认被误封顶为 1 的 bug（2026-09-05）。桌面端需重启生效（main/preload 变更） |
\| ED-28 | 文件树右键菜单（新规格） | **目录**：在资源管理器打开 / 新建目录(该目录下) / 新建笔记(该目录下) / 删除 / 移动；**笔记**：在资源管理器打开 / 新建笔记(同级目录下) / 删除 / 移动；**空白区**：新建目录(根目录下) / 新建笔记(根目录下) / 显示隐藏目录；统一 stopPropagation 阻断冒泡到 document，与 app-vault.js / app-layout.js 旧菜单不再叠加；doNewNote/doNewFolder 接受目标目录参数 | 测试完成 | 2026-09-04 | 2026-09-05 | 按用户新规格重写 file-tree-ctx.js；openInExplorer 统一调 noteDesktop.revealNote（main.js shell.showItemInFolder 对文件/目录都支持）；旧 showNoteContextMenu/showBlankContextMenu 等函数保留但不再被调用；语法校验通过（2026-09-05）                                                                                                                                                                                                       |

**【新增】运行日志落盘 + 启动防崩（2026-09-05）**：解决"升级后界面空白、按钮全部无效"——定位根因：`app-layout.js` 被错误覆盖成了 app-vault.js 的副本，丢失原 318 行的启动引导（`loadView`/`init`/`DOMContentLoaded 钩子`/`initEditor()` 调用/hashchange 路由/宽度持久化等），视图不装载 → 空白。修复：从 git HEAD 恢复 `app-layout.js` 完整版（保留三套新菜单接管，剔除多余 `bindNoteContextMenu()` 调用）。同时新增能力（防复发 + 可排查）：① 新增 `js/app-log.js`（置于 index 脚本区最前）——全局捕获 `error`/`unhandledrejection`，提供 `window.SBLog` 接口，出现错误时页面右下角显示红色计数徽标（点击弹最近日志）；② `preload.js` 暴露 `noteDesktop.log(level,msg,detail)`，`main.js` 增加 `app:log` IPC 落盘 `userData/logs/app.log`（5MB 轮换）；③ 网页端降级写 `localStorage[sb:log]` 环形缓冲（200 条）；④ `app-layout.js/bindViewInteractions` 中每个视图初始化用 `safeInit` try/catch 隔离，单视图异常只记录不再拖垮全局 | 测试完成 | - | 2026-09-05 | `node --check` 全部通过；`test_load.js` 冒烟：13 个脚本全部加载、启动无未捕获异常（jsdom 无法复现视图装载，已在脚本内注明该局限） |

**【运行期问题 · 日志驱动排查（2026-09-05）】** 用户反馈左侧按钮未显示 + 新建目录/笔记报错。读取落盘日志 `userData/logs/app.log` 定位到两处，均在启动引导/Electron 能力层面：

1. **新建目录/笔记报错：Electron 不支持** **`window.prompt`**。日志反复出现 `prompt() is not supported`（来源 `doNewFolder`@app-editor:901、`doNewNote`@app-editor:880）。Electron 渲染进程禁用原生 `window.prompt/alert/confirm`，新建一弹输入框即抛错。

   - 修复：新增 `js/app-input.js`（作者 火 冰）提供 DOM 输入模态 `window.inputModal(opts)->Promise<string|null>`；`app-editor.js` 的 `doNewNote/doNewFolder`、`app-editor-ctx.js` 的 `mdExternalLink`（插入内部/外部链接）共 6 处 `prompt()` 全部改为 `await window.inputModal(...)`；全局 `prompt(` 已清零。

   - 连带修复 `confirm`：Electron 中 `confirm()` 返回 false 会让删除静默失效。`preload.js` 暴露同步 `noteDesktop.confirm`，`main.js` 新增 `dialog:confirm` IPC（原生 `showMessageBoxSync`），`app-input.js` 覆盖 `window.confirm`，删除笔记/目录确认恢复正常。
2. **左侧按钮整列不显示：`RibbonManager.init()`** **从未被调用**（全局无调用点）→ `render()` 不执行。此前的启动引导遗漏了对 RibbonManager 的初始化。

   - 修复：在 `app-layout.js` 的 `init()` 中补 `RibbonManager.init('file')`（用 `safeInit` 隔离防崩）。 | 测试完成 | - | 2026-09-05 | 全量 `node --check` + `test_load.js` 冒烟 PASS；需桌面版（Electron）重启后生效（preload/main 变更），遗留校验项：重启后左侧按钮出现、新建/删除/插入链接不再报错 |
     \| ED-29 | 文件树拖拽移动+排序    | HTML5 drag：笔记拖入目录 → 移入；笔记/目录拖到根空白处 → 移出；禁止目录拖入自身子目录；**同目录内笔记互拖 → swap（交换两个文件名，保留内容不变，链接 \[\[A]] 指向正确）**；视觉反馈用硬编码 rgb(124,58,237,.12) 避免 --rgb 变量缺失失效                                                                                         | 测试完成 | 2026-09-04 | 2026-09-04  | jsdom 端到端 36 断言 + swap 单元测试通过；renderFileTree 输出 draggable 元素；#file-tree 事件委托 dragstart/dragover/drop/dragend；handleFileTreeDrop 分支同目录 swap；CSS 硬编码 rgba 修复视觉反馈失效（2026-09-04）；**2026-09-05 修复拖拽 3 类问题**：空白区 drop 加节点命中提前 return（解决根目录笔记拖到目录误报「原地拖放无变化」）；主进程新增 notes:moveDir（fs.rename 整目录移动并移除源目录），noteStore.moveDir 改走桥接（解决空目录报 0 篇、源目录残留），详见 历史Bug记录.md Bug-001        |
     \| ED-30 | 右侧面板拖拽调宽      | 面板左缘拖拽条调宽(220-440px，桌面端)                                                                                                                                                                                                                     | 测试完成 | 2026-09-03 | jsdom 14/14 | 14/14 断言全通过                                                                                                                                                                             |
     \| ED-31 | 目录区/侧边面板宽度持久化 | treeWidth/sideWidth 存 localStorage，启动恢复上次宽度                                                                                                                                                                                                  | 测试完成 | 2026-09-03 | jsdom 14/14 | 14/14 断言全通过                                                                                                                                                                             |

***




***




***




***




***




***




***




***




***




***





***






***




***




***

