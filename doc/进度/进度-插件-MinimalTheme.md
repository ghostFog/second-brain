# Minimal Theme 插件 · 已完结进度

> 归属：单个插件功能归档。插件框架/规范功能见 `进度-插件.md`（开发进度入口见 `../开发进度.md`）。

- 插件目录：`plugins/minimal-theme/`，当前 manifest 版本 `1.0.0`
- 能力：极简主题包，支持 纸白/亚麻/冷灰/墨绿 4 套内置配色与整套可调的自定义主题，顶栏按钮悬浮弹出主题列表；经宿主抽象接口协同管理编辑器（Vditor）主题（每套配色可配深浅 + 注入配色 CSS）

| 编号 | 功能点 | 功能点详情 | 状态 | 开始时间 | 完成时间 | 记录 |
| ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| PL-18 | Minimal Theme 自定义配色 + 顶栏悬浮列表 | Minimal Theme 支持整套配色可调的自定义主题；顶栏主题按钮悬浮弹出可用主题列表 | 测试完成 | 2026-09-06 | 2026-09-06 | plugins/minimal-theme/main.js 重构：新增 getSetting/readCustomColors/buildCustomVars/applyCustomVars/clearCustomVars（自定义主题以 body 内联 CSS 变量注入 8 项可调色值 + 派生全量变量）、applyTheme 增加 'custom' 分支与 silent 参数（启动静默恢复）、mapDefaultToId 把「默认配色方案」下拉值映射为 id；顶栏悬浮：watchToolbar 用常驻 MutationObserver 按元素去重重绑按钮（兼容 ToolbarManager.render 重建 DOM），悬浮弹出 .mt-theme-dropdown 列表（停用/纸白/亚麻/冷灰/墨绿/自定义配色，点击应用并关闭，受 hoverToolbar 开关控制）；监听 plugin-setting-changed 实时重套自定义色值/套用默认配色。host 侧 app-settings.js renderSchemaForm 新增 type:"color" 原生取色器分支（向后兼容，value 存 HEX）。manifest.json 新增 apply-custom 回调、defaultTheme 选项「自定义配色」、8 项 color 设置字段（cBg/cCard/cSurface/cBorder/cInk/cInk3/cLine/cBrand）；命令面板收敛为单个「Minimal: 切换主题配色」（cycle-theme）——快捷键只绑定切换功能，不按主题各设命令。smoke-plugin.test.js 新增 color 取色器渲染断言 + 隔离 dom 注入最小主题插件验证（加载无异常/apply-custom 注册/悬浮弹列表/自定义套用写内联变量/停用清除），并校正陈旧断言「基本设置」→「基本信息」与 ST-23 一致；smoke 39/39、回归 5/5、node --check 全过 |
| PL-21 | Minimal Theme 管理编辑器（Vditor）主题（抽象接口） | 把 Vditor 主题暴露给 Minimal Theme 管理：宿主提供抽象解析器 + 注入通道，插件按每套配色可配深浅并注入配色 CSS，插件不直接调 vditor | 测试完成 | 2026-09-09 | 2026-09-09 | 抽象：host editor-vditor.js 新增 resolveVdTheme()（遍历 window.__hostThemeResolvers 取首个合法 {theme,extraCss}，无则回落 isDarkTheme）与 applyVdExtraCss()（持久 #host-vditor-theme-extra <style> 注入），syncVdTheme 改走解析+注入（保留 window.vdSyncTheme/vdResolveVdTheme），buildVditor theme 用 resolveVdTheme().theme 并在构建后注入 extraCss；host app-plugins.js 新增 PluginAPI.registerEditorThemeResolver()（可注销）与 PluginAPI.editor.theme{get,sync} 抽象桥。minimal main.js 注册 editorThemeResolver：按当前配色经 edTheme1-4/edThemeCustom（自动(跟随宿主)/浅色/深色）决定 Vditor 深浅，injectCss 开启时 buildEditorCss 把宿主 --note-* 变量映射为 vditor 覆盖样式（深色交 vditor 内建）、extraCss 可手写追加；applyTheme 与编辑器主题类设置变更后调用 PluginAPI.editor.theme.sync() 通知宿主重算。manifest.json 新增 5 个编辑器主题 select、injectCss 开关、extraCss 文本。验证：node --check 全过；smoke 92/92、回归 59/59；同时校正两处陈旧断言（app-note.js 的 renderMarkdown 改从全局词法作用域取、语言只写 data-lang 不再生成可见 code-lang 标签）；后续 Bug-034 修复：注入选择器统加 html body 前缀（特异度 > vditor 动态后加载的自带规则）+ applyVdExtraCss 每次重挂 head 末尾，使编辑器背景真正跟随配色；并延续「内容层主题化」：buildEditorCss 拆分 UI 壳与内容层（lightContentCss/darkContentCss），编辑区 table/引用/代码块浅色态用 --note-* 跟随配色、深色态用与宿主协调的固定暗色，消除任何模式下表格刺眼白底（冒烟 95/95、回归 59/59、node --check 过）；进一步按「Minimal↔Vditor 映射关系」补全编辑层：链接→--note-brand、分隔线(hr)→--note-line、callout/卡片→--note-card（浅色态 var、深色态固定暗值），现有变量即可承载、无需新增；冒烟 98/98、回归 59/59、node --check 过 |