# 第二脑 · 开发进度跟踪表

> 用途：按模块跟踪每个功能点状态。
> 状态取值：计划 -> 设计 -> 编码 -> 开发完成 -> 测试完成 -> 完成。
> 维护约定：**已完结历史已归档到 `进度/进度-<模块>.md`，本文件只保留未完结条目**与模块导航。
> 新增功能点：先登记到下方「未完结看板」，完成后再移入对应模块子文件。

***

## 未完结进度（看板）

| 模块 | 编号 | 功能点 | 状态 | 记录 |
| ---- | ---- | ---- | ---- | ---- |
| 全局框架 | G-13 | 多窗口知识库 | 开发完成 | 打开/切换/恢复库新开主窗口，单库单窗口约束（已有窗口聚焦）；窗口标题带库名；窗口级最小化/关闭；IPC 按发起窗口解析库根（AsyncLocalStorage）；迁移默认库同步刷新受影响窗口。npm test 163 通过（含 Bug-052 回归）；Bug-054 修复：多窗口关闭按钮只关当前窗口（不再按 closeAction=quit 退出全部），单窗口关闭正常退出已冒烟验证，多窗口交互待手动验证 |
| 全局框架 | G-14 | 托盘知识库列表 / 主进程异常处理 | 开发完成 | 托盘右键菜单列出全部知识库（默认库+打开库+历史库去重），点击打开对应库主窗口（单库单窗口聚焦），窗口创建/关闭、库切换/迁移/移除时实时刷新；主进程 uncaughtException 落盘 userData/logs/app.log + 可复制错误弹窗（textarea 右键复制 + 日志路径 + 打开日志目录，preload 新增 openLogDir）。手动验证：关闭主窗口正常退出无「Object has been destroyed」报错（Bug-053 修复），npm test 163 通过 |
| 全局框架 | G-15 | 日志面板清除 | 开发完成 | 运行日志弹框右上角新增「清除」（红）+「关闭」按钮组；点「清除」清空本会话日志缓冲、重置错误计数、移除右下角红色徽标（#sb-log-badge）、日志文本域随之清空，之后仍可继续记录新日志。node --check 语法通过；npm test 163 通过 |
| 全局框架 | G-16 | 浮库菜单二级操作 | 开发完成 | 库切换悬浮菜单知识库列表直接作为一级菜单，每个库条目（当前库+历史库）右缘悬浮 ⋯ 弹出二级菜单（删除/迁移/设置默认）。删除=仅从历史移除（不删文件）；迁移=迁移默认知识库（移动语义）；设置默认=把该库设为新的默认库（新增 vault:setDefault IPC，preload 暴露 setDefaultVault，不迁移文件仅持久化 defaultVaultPath）。移除底部一级「迁移默认知识库…」。node --check 语法通过；npm test 163 通过，交互待手动验证 |
| 知识库 | ED-10 | 更多操作 | 计划 | 占位，待实现 |
| 知识库 | ED-21 | 标签云 | 开发完成 | 点击筛选占位 |
| 知识库 | ED-22 | 宿主 Shell 拆分（js/editor 独立目录 + Provider 契约） | 测试完成 | 核心/宿主/tab/目录/侧边/右键拆分；编辑器能力由后缀对应插件接管 |
| 知识库 | ED-24 | 链接：插入/编辑/移除弹框（inputLinkModal，docButton 复用） | 完成 | 解决 docButton not defined，见 Bug-011 |
| 插件 | MD-03 | 文件拖拽临时打开 | 计划 | 新增需求，待实现 |
| 插件 | MD-04 | 链接/反向链接数据管理 | 开发完成 | 前端消费主进程链接索引：renderBacklinks 改用 fileMeta(backlinks)、buildGraph 改用 linksIndex(outlinks)、编辑区内链接点击按内部/外链分流（新开页签）；相对路径按源笔记路径解析归一（./ ../ 同目录 / 开头从库根；wiki 纯名保持全局匹配）；新增 notes:linksIndex IPC |
| 命令面板 | CMD-03 | 新建笔记 | 开发完成 | 入口展示 |
| 命令面板 | CMD-04 | 打开笔记 | 开发完成 | 入口展示 |
| 命令面板 | CMD-05 | 搜索笔记 | 开发完成 | 入口展示 |
| 命令面板 | CMD-06 | 导出为 PDF | 设计 | 待实现 |
| 命令面板 | CMD-08 | 浏览插件市场 | 开发完成 | 跳转 |
| 命令面板 | CMD-10 | 同步设置 | 设计 | 待实现 |
| 命令面板 | CMD-12 | 结果计数 | 开发完成 | 随过滤更新 |
| 图谱视图 | GR-10 | 脉冲动画 | 开发完成 | CSS 装饰 |
| 图谱视图 | GR-11 | 图例 | 开发完成 | 渲染 |
| 图谱视图 | GR-12 | 操作提示 | 开发完成 | 渲染 |
| 图谱视图 | GR-13 | 面板标题 | 开发完成 | 渲染 |
| 图谱视图 | GR-15 | 基本信息 | 开发完成 | 渲染 |
| 图谱视图 | GR-16 | 标签 | 开发完成 | 渲染 |
| 插件 | PL-07 | 量化指标 | 开发完成 | 渲染 |
| 插件 | MD-06 | 表格列宽模式切换按钮 + 编辑器查找替换（Ctrl+F/R） | 测试完成 | 已归档：表格列宽按钮见 `进度-插件-MarkdownEditor.md` MD-06、查找替换见 MD-09（回归 216 通过） |
| 设置 | ST-21 | Web 服务开关 | 计划 | 新增需求，待实现 |
| 设置 | ST-22 | Web 服务选择 | 计划 | 新增需求，待实现 |
| 设置 | ST-23 | Web AI 问答 | 计划 | 新增需求，待实现 |
| 设置 | ST-26 | 快捷键录制交互 | 测试完成 | 点设置进入「按下组合键」；点其他处/Esc/点另一项退出并还原显示；录制时按键不触发全局快捷键（stopPropagation） |
| 设置 | ST-36 | Agent 配置（自定义角色） | 开发完成 | 设置-AI 新增「Agent 配置」区块：Agent 列表（名称 + 提示词首行预览 + 当前标记 + 编辑/删除）+ 添加/编辑弹层（名称 + 系统提示词 textarea + 温度/top_p/最大 token 三参数，空串或非法值按未配置处理），存 cfg.agents/currentAgentId（与模型列表同走 saveConfig/persist）；问答页底部输入区新增 Agent 选择器（与模型选择器并排，bot 图标），切换保存 currentAgentId，发送经 ai.ask(question, history, agentId) 链路（app-ai → preload → main ai:ask → engine.ask）传 agentId；引擎按 agentId 解析 Agent 并把 systemPrompt 前置注入（未配置走默认 SYSTEM_PROMPT），生成参数 _agentParams 合并进请求：OpenAI temperature 覆盖默认 0.7 + top_p/max_tokens 按需携带，Ollama options 合并 temperature/top_p/num_predict（与模型级 num_ctx 并存）；设置二级菜单新增「Agent 配置」子项（滚动到 ai-sec-agent）。npm test 166 通过，待手动交互验证 |
| 设置 | ST-37 | 打印完整提示词（调试） | 开发完成 | 设置-AI「生成模型」区块新增滑动开关「打印完整提示词」（cfg.printFullPrompt，默认关，随 collectLocal/persist 自动保存/回填；开关 change 即 persist 持久化——修正 Bug：初版未绑定 change 导致切走 ribbon 面板再回来开关还原）；引擎 ask() 组装完 messages 后开启时 console.log('[调试·完整提示词] ' + JSON.stringify(messages, null, 2))。主进程日志可见性：main.js 桥接 console.log → 所有窗口 webContents 'main:log'（保留终端输出），preload 暴露 noteDesktop.onMainLog，渲染进程 app-log.js 监听后 SBLog.info（日志面板/落盘）+ console.log（F12 可见）——桌面版引擎在主进程，不桥接则 F12 控制台看不到。npm test 173 通过，待手动验证控制台输出 |
| 设置 | ST-38 | 携带历史数据（AI 问答） | 开发完成 | AI 问答顶栏新增「携带历史」滑动开关（#ai-carry-history，默认 checked，回填 cfg.carryHistory !== false 视为携带）+ 设置-AI「生成模型」区块同步新增同名开关（data-ai-cfg="carryHistory"，change 即 persist），两处同写 cfg.carryHistory（DEFAULT_CONFIG 默认 true）；发送处 `const carry = (carryEl && !carryEl.checked) ? [] : s.history.slice(0, -1)`，关闭时 ai.ask(q, [], agentId) 每轮只发当前问题（界面历史气泡保留显示）。npm test 177 通过（ST-38 新增 4 条断言），交互待手动验证 |
| 设置 | ST-39 | Agent 增强：默认 Agent + 使用知识库 | 开发完成 | 引擎内置默认 Agent「知识库助手」（DEFAULT_AGENT：id=kb-assistant、systemPrompt=初始化介绍、useKnowledge=true、builtin=true；DEFAULT_CONFIG.agents 默认含之、currentAgentId 默认 kb-assistant）；currentAgent() 按 id 解析→未命中回退列表首个→列表为空回退内置；ask() 按 `agent.useKnowledge !== false` 决定是否检索知识库并把笔记片段拼入系统提示词（false 不检索不拼接），agent.systemPrompt 即完整系统提示词（不再叠加 SYSTEM_PROMPT）；设置-AI Agent 弹层加「使用知识库」开关（data-ai-form=useKnowledge，回填 item.useKnowledge !== false），内置 Agent 名称禁用（保持「知识库助手」）、删除按钮替换为「还原」（ai-agent-reset 还原 AGENT_DEFAULT_PROMPT + useKnowledge=true + 清参数）、removeAgent 拦截内置删除（提示不可删除）；AI 问答顶部工具栏「AI 问答」标题改 Agent 长下拉（#ai-agent，bot 图标，填充 agents 切换保存 currentAgentId），底部输入区 Agent 选择器删除（保留模型选择器）。npm test 185 通过（ST-39 新增 5 条断言），交互待手动验证 |
| 设置 | ST-40 | 可用模型实时列表（后台轮询 + 全局缓存） | 开发完成 | ①引擎新增可用模型**全局缓存** `_available`：`refreshAvailableModels()` 按已配置 baseUrl 去重，Ollama 走 /api/tags、OpenAI 兼容走 /models（`listOpenAiModels`），失败标记 error；`startModelPolling` 改为同时刷新运行状态 + 可用模型，**由 main.js 在 initAiEngine（配置加载完成后）调用**（修复原启动时 cfg.models 未加载空跑导致设置页打开几秒后才有状态）；`getAvailableModels()` + `ai:availableModels` IPC + preload 桥接；②**设置-AI「可用模型（实时）」区块已移除**（用户反馈不需要：settings-panels 删除 #ai-avail-models/#ai-avail-ts，settings-ai 删除 renderAvailableModels 及其在 startAutoRefresh/tick/钩子中的调用，设置页仅保留生成模型列表运行状态刷新）；③AI 问答页模型下拉**显示「已加载到内存（运行中）」的模型**：配置运行中（listModels running=true，本地 Ollama 仅运行中/远程 OpenAI 始终可用）立即渲染 + 经 `getRunningMap()` 运行状态缓存合并**未配置但已加载**的模型（`avail:<baseUrl>:ollama:<model>` 临时 id，标题标「已加载到内存」，模型卸载即从下拉移除；不再合并服务端 tags 全部可用模型），每 5 秒 `__aiModelSyncTimer` 同步（生成中不刷新）；④ask 链路支持 `modelId`：app-ai `ai.ask(q, carry, agentId, modelId)` → preload → main ai:ask → engine.ask，`_modelById` 对 avail id 从 _available 构造临时模型（apiKey 取同 baseUrl 已配置项）。npm test 196 通过（ST-40 断言更新：设置页断言改为确认可用模型区块已移除），待重启应用真机验证 |
| 设置 | ST-41 | 模型数据钩子实时刷新（类 Vue 响应式） | 开发完成 | 引擎新增 `onModelsUpdated(cb)`/`_notifyModelsUpdated()`（回调数组 `_modelCbs`，异常隔离），`startModelPolling` 的 run 改 `Promise.all([refreshRunningMap, refreshAvailableModels]).then(通知)`（每次轮询/手动刷新完成触发钩子）；main.js `initAiEngine` 注册 `aiEngine.onModelsUpdated` 回调 → 向所有窗口推送 `ai:models-updated` 事件；preload 暴露 `onModelsUpdated(cb)`（removeAllListeners + on 监听）；设置-AI `startAutoRefresh` 与 AI 问答页 `refreshOnce` 均监听该钩子**立即重绘/重刷下拉**（5 秒定时器保留兜底）——数据变化即显示，不等定时器；同时三处 fetch 查询超时 `AbortSignal.timeout(10000)`→`3000`（Ollama /api/tags、/api/ps、OpenAI /models），服务不可达 3 秒内快速失败并显示具体 error（消除「打开设置/AI 问答约 10 秒才显示状态」）。npm test 196 通过（ST-41 新增 5 条断言），待重启应用真机验证实时刷新与 fetch failed 排查 |
| 设置 | ST-42 | 开发者模式（接管打印提示词） | 开发完成 | 设置-常规-通用新增「开发者模式」开关（`data-skey=devMode`，localStorage note-app:settings.devMode，副作用提示）；设置-AI「打印完整提示词」独立开关移除，改由开发者模式接管（引擎 ask 打印条件 `devMode || this.cfg.printFullPrompt` 兼容旧配置）；AI 问答回答统计（检索时长/首token/每秒token/总耗时）与资料来源相关度分数**仅开发者模式显示**，首token 拆分为「检索索引时长」+「真正首token」（引擎 ask 返回 retrieveMs，前端 firstTokenMs − retrieveMs）；主进程 ai:ask 透传 devMode 并把 retrieveMs 经 ai:ask-done 回传、preload ai.ask 透传 devMode、app-ai aiSend 读 aiDevMode() 传入；历史会话重绘沿用持久化 retrieveMs 字段；参考资料项新增「查看完整索引块」按钮（引擎 retrieve 返回 fullText 完整块文本，`aiShowSourceModal` 弹窗 pre 展示）随开发者模式显示。npm test 207 通过（ST-42 8 条 + ST-42b 新增 3 条断言），交互待手动验证 |
| 设置 | ST-43 | 知识库检索相关度阈值 | 开发完成 | 引擎新增 `cfg.minSimilarity`（0~100，`DEFAULT_CONFIG` 默认 0=不过滤），`retrieve()` 排序后按 `c.sim*100 >= minSimilarity` 过滤低相关度块再取 topK（低于阈值不作为参考资料）；设置-AI「知识库索引」新增「相关度阈值」数字输入（`data-ai-cfg=minSimilarity`，随 collectLocal/saveConfig 持久化）；AI 问答顶部工具栏「携带历史」旁新增 `#ai-min-sim` 相关度≥ 输入，app-ai 用 `getConfig()` 回填、`change` 即 `saveConfig({minSimilarity})` 并状态提示。npm test 212 通过（ST-43 新增 5 条断言），待重启应用真机验证
| 平台规划 | RIB-01 | Ribbon 空白区右键二级菜单 | 开发完成 | Ribbon 空白区右键改二级菜单：一级两项（Ribbon 最大显示个数 › / 开发人员工具 ›），hover/点击行右侧展开子菜单；ribbon 子菜单 7~15 数值（当前打勾）+ 恢复默认顺序（沿用 setMaxButtons/resetOrder）；devtools 子菜单「打开/关闭开发者工具」走 noteDesktop.toggleDevTools（IPC → main win:devtools）；外部点击关闭一级+二级（closeBlankAreaMenu 先关子菜单）。npm test 172 通过（ST-37 新增 3 条 ribbon 断言），交互待手动验证 |
| AI 问答 | AI-17 | AI 创建笔记 | 计划 | 新增需求，待实现 |
| AI 问答 | AI-18 | 后台运行与恢复 | 计划 | 切换 Ribbon 生成不中断，返回视图保留进行中气泡与累计内容 |
| AI 问答 | AI-19 | 回答统计 | 开发完成 | 回答统计增强：气泡尾部统计行新增 **每秒 tokens（X.X tok/s）**（= 累计 token 数 ÷ 生成耗时）；生成中顶部状态实时显示「生成中 · X.X tok/s」（onToken 每 chunk 计 1 token：aiTokCount 累计，节流 500ms 更新状态栏避免每 token 重绘，aiSend 每次发送前清零）；aiAppendMeta 支持 tokens/tokSec 字段。npm test 196 通过，待真机验证 |
| AI 问答 | AI-24 | 按组删除问答 | 开发完成 | 消息渲染改为问答组容器（.ai-qa-group + aiCurGroupEl：user 开启新组、assistant 归入当前组）；每组右上角 hover 显示「删除本组」（trash-2），aiRemoveGroup 按 DOM 组序号定位 history 区间（该 user 起至下一 user 前）→ splice → 持久化 → renderSessionChat 重绘 + aiRebuildGeneratingBubble 恢复进行中气泡；生成中删当前组先 ai.stop() 并复位生成状态（aiCurText 已清空，aiFinish 不再写历史）；删除后空历史回空态引导。npm test 179 通过（AI-24 新增 2 条断言），交互待手动验证 |
| AI 问答 | AI-25 | 语义检索绑定当前知识库 | 开发完成 | AI 问答用知识库检索时绑定「当前打开的知识库」的索引：引擎新增 `_ensureCurrentIndex()`，retrieve 前检测内存索引库根(vaultPath)与发起窗口当前库根(_currentRoot())不一致即重载该库索引（activateVault 懒激活），并同步快照该库索引供异步 embed/rerank 使用、避免多窗口检索并发互相重载覆盖；解决多窗口/多库下非默认库窗口问答误检默认库/上次库索引的问题。npm test 216 通过，待真机多库验证 |
| AI 问答 | AI-20 | 生成打断 | 计划 | 生成中提供「停止」，打断后保留部分内容并标记已打断 |
| AI 问答 | AI-21 | 可用模型过滤 | 计划 | 选择器仅显示 Ollama 运行中 + 全部远程 OpenAI 模型 |
| AI 问答 | AI-22 | 模型常驻 | 开发完成 | 手动/问答带 keep_alive=-1 常驻到手动卸载 + 「启动常驻」开关（默认开）启动时自动常驻最后使用问答模型；逻辑+API 已验证，待重启应用真机确认常驻 |
| AI 问答 | AI-23 | 会话持久化 | 开发完成 | 会话落盘到知识库根 .session（每会话一 json + active.json）；消息/标题/删除/切换即时写盘，启动自动恢复上次会话；语法+回归 163 通过，待真机确认 .session 生成与重启恢复 |
| 平台规划 | SEC-01 | 启动密码 | 计划 | 新增需求，待实现 |
| 平台规划 | SEC-02 | 超时锁定遮罩 | 计划 | 新增需求，待实现 |
| 平台规划 | ENC-01 | 加密/解密文件 | 计划 | 新增需求，依赖 PL-10/11/12 |
| 平台规划 | GIT-01 | 初始化仓库 | 开发完成 | Git Sync 插件：设置弹框「初始化仓库」执行 git init；git 经宿主白名单 IPC 桥接。npm test 159 通过，待手动交互验证 |
| 平台规划 | GIT-02 | 自动提交 | 开发完成 | Git Sync 插件：定时器按间隔自动 add+commit（默认 10 分钟、可在设置弹框调整）；可开关。npm test 159 通过，待手动交互验证 |
| 平台规划 | GIT-03 | 远程仓库配置 | 开发完成 | Git Sync 插件：设置弹框可设置/更新 origin 地址。npm test 159 通过，待手动交互验证 |
| 平台规划 | GIT-04 | 推送/拉取 | 开发完成 | Git Sync 插件：「立即同步（提交+推送）」与设置弹框「拉取」；自动推送为可配开关（默认关）。npm test 159 通过，待手动交互验证 |
| 平台规划 | GIT-05 | 查看/还原版本 | 开发完成 | Git Sync 插件：命令「查看版本历史」弹框列最近版本 + Diff/还原到此；文件树右键「查看版本历史」；还原用 git checkout 覆盖工作区，不动分支/历史。npm test 159 通过 + 临时目录验证 checkout 语义，待手动交互验证 |
| 平台规划 | GIT-06 | 同步到仓库（自动/手动） | 开发完成 | 见 GIT-02/04 记录；自动推送开关（默认仅自动提交） |
| 平台规划 | GIT-07 | 从 Git 打开知识库 | 开发完成 | Git Sync 插件：命令「从 Git 打开知识库…」输入 URL → host 选目录克隆为独立库并新开窗口；临时目录验证 clone。npm test 159 通过，待手动交互验证 |
| 平台规划 | GIT-08 | 宿主 Git 桥接（白名单） | 开发完成 | main.js 新增 git:run/git:check、vault:cloneGit IPC + preload noteDesktop.git/cloneGit；子命令白名单 + cwd 锁定知识库根，execFile 非 shell 规避注入。npm test 159 通过 |
| 平台规划 | GIT-09 | 入口并联 | 开发完成 | 命令面板（新增 git-file-log/revert）+ 文件树右键（host file-tree/slot 合并插件菜单 + 注入 __pluginCtxNote/Folder）+ 左下常驻状态条三处暴露 Git 能力。npm test 163 通过，右键入口待手动验证 |
| 平台规划 | GIT-10 | 单文件历史/还原 | 开发完成 | fileLogModal 改左右布局：左列仅 hash+时间+「还原」，点击在右列对比框展示该版本 diff，顶部滑动开关（开=对比本地 / 关=对比上一版本），弹框加大；删除独立「查看」按钮；右键菜单仅留「查看此文件历史」；host 在 action 执行前恢复 __pluginCtxNote 修复右键取不到目标笔记。回滚：还原/回滚成功后宿主 editor.reloadNote 刷新编辑器到磁盘、onEdInput 仅在内容变化且非脏时写盘（Bug-055，待手动验证）。npm test 163 通过，待手动交互验证 |
| 平台规划 | GIT-11 | 还原彻底性 | 开发完成 | 整库还原弹框带「同时清理未跟踪文件/空目录」复选 → git clean -fd（host 白名单新增 clean）；临时目录验证 clean -fd 移除 untracked+空目录、保留 staged/已跟踪。npm test 163 通过 |
| 平台规划 | GIT-12 | 历史改动文件 | 开发完成 | 历史弹框改 --log --name-status 展示每次提交文件清单（M/A/D/R 色标），点文件看 git show <hash> -- <path> 单文件 Diff。npm test 163 通过 |
| 平台规划 | GIT-13 | 常驻 Git 状态指示 | 已取消 | 左下贴边常驻状态条遮挡目录区，取消该需求；状态反馈收敛到目录树状态着色（GIT-14，监听 #file-tree 重绘→文件变动即时调色）。不再渲染状态条（删除 ensureStatusBar/showStatus/.gitsync-statusbar 样式） |
| 平台规划 | GIT-14 | 目录树状态着色 | 开发完成 | status --porcelain -z 解析 → 全局着色映射 window.__gsColoring（path → untracked/staged/modified）；宿主 renderFileTree 渲染 .tree-file 时直接拼 gs-* class（渲染即带状态色，无补色闪烁），已打开高亮节点不上状态色；插件状态周期/动作后更新映射并就地兜底补色；目录不再橙色（移除 gs-dirty）。npm test 163 通过，待手动交互验证 |
| 平台规划 | GIT-15 | 过滤设置（编辑 .gitignore） | 开发完成 | Git Sync 设置弹框新增「编辑 .gitignore」：读库根 .gitignore 到文本域、保存写回（notes:read/save，manifest 加 vault.write）；git add . 天然尊重 .gitignore，忽略路径不进自动提交。npm test 163 通过，待手动交互验证 |
| 平台规划 | GIT-16 | 实时提交 & Ribbon 同步 | 开发完成 | 设置弹框新增「实时提交」开关（文件变动即提交本地）；经 #file-tree MutationObserver 防抖触发静默 commitNow(false)。Ribbon 按钮改为同步：宿主 git-sync ribbon 加 actionKey=git-sync → syncNow（未初始化 confirm→initRepo；已初始化→commitNow(autoPush)）。GIT-13 已取消，状态反馈收敛到目录树即时着色（文件重绘触发生成着色/调色）。npm test 163 通过，待手动交互验证 |
| 平台规划 | GIT-17 | 右键 Git 二级菜单 | 开发完成 | 文件树右键笔记 → 「Git」二级菜单：对比(diff HEAD -- path 弹框)/历史(fileLogModal)/回滚(checkout -- path，confirm)/提交(add -- path+commit)/拉取(pull)/推送(push)；对比与历史弹框高度固定(gitsync-modal-lg height:76vh)。宿主 app-plugins.js contextMenus 支持 children 递归转换、file-tree-ctx.js 递归恢复 __pluginCtxNote；manifest 声明父项 Git+6 子项。npm test 163 通过，待手动交互验证 |
| 平台规划 | GIT-18 | 合并提交 & 空白区 Git 菜单 | 开发完成 | 笔记右键 Git 加「合并提交」=仅把该文件相对跟踪远端的未推送改动合并为一笔（reset mixed → add -- path → commit，其它文件改动留工作区不丢）；目录区空白处右键新增「Git」整仓二级菜单（提交/合并提交/拉取/推送），整仓合并=reset --soft upstream → commit（内容全保留仅压缩历史）。目录本身右键新增「Git」菜单（提交/合并提交/推送，针对该目录，resolveDirTarget 走 __pluginCtxFolder 可为根 ''；目录合并=reset mixed → add -- <dir> → commit 仅含该目录、目录外改动留工作区）。上游由 rev-parse @{upstream} 解析，未配置跟踪分支时提示。host main.js 白名单加 reset 并拦截 reset --hard；file-tree-ctx.js 空白区挂 file-tree-blank、目录挂 file-tree-folder slot；manifest 新增两槽（gs-ctx-repo-* / gs-ctx-dir-*）。npm test 163 通过 + 临时目录验证整仓/单文件/目录合并语义，待手动交互验证 |
| 平台规划 | GIT-19 | 初始化创建默认过滤文件（.gitignore） | 开发完成 | initRepo 在 git init 成功后读取库根 .gitignore（notes:read，不存在抛错）→ 不存在则写默认忽略规则（notes:save，DEFAULT_GITIGNORE：忽略 .second-brain/.session/.obsidian/.trash 与临时/日志/系统文件），已存在则保留用户内容；.resources/（上传资源文件）需版本管理，不忽略。npm test 163 通过，待手动交互验证 |
| 插件(知识库) | MD-07 | 大纲移至 vditor 浮层 + 快捷键 | 测试完成 | ① 侧边面板移除大纲区块：`views/vault.html` 删除 ed-outline 区块、`editor-host.js` renderArticle 移除大纲渲染、插件/宿主 sidebar 注册移除 outline 项；② Markdown Editor 插件新增命令「显示大纲」(`mde-outline`，`manifest.json` commands 声明默认快捷键 `Ctrl+Shift+Q`)，经 `window.vdToggleOutline` 复用 vditor 内置外轮廓浮层（editor-vditor.js 新增该桥接，点击 vditor 工具栏 outline 按钮同位逻辑）。npm test 163 通过，UI 待手动验证 |
| 插件(知识库) | MD-08 | 编辑器宽屏模式 + 表格 100%（功能与按钮均归插件） | 测试完成 | 功能与工具按钮均迁入 Markdown Editor 插件：插件 main.js 实现 `mde-widescreen`（挂 `html/body.ed-wide`，记忆键 `sbWide`，命令面板 action + `MutationObserver` 监听 `#ed-plugin-wide-slot` 注入槽渲染「宽屏」按钮并在宿主 loadView 重建编辑视图 DOM 时重注入 + 按记忆恢复 + 按钮高亮）；插件 styles.css 迁入宽屏（`html.ed-wide` 编辑区宽度 100% + 清水平留白）与表格 100%（`.vditor-reset table{display:table}` + 单元格 `word-break`，不横向溢出、不做容器滚动）样式；宿主移除硬编码按钮/绑定/命令/CSS。**修复**：vditor 在 IR 模式给 `pre.vditor-reset` 内联 `padding:10px 35px`、预览给 `.vditor-reset` 内联 `max-width:800px`（inline 样式普通样式表盖不掉），宽屏下统一 `!important` 覆盖 `padding:0 8px` + `max-width:100%`，避免内容向里挤、预览受限 800px；按钮图标改为左右箭头 `move-horizontal`（区别于全屏）。npm test 179 + 冒烟 122 通过，UI 待手动验证 |

***

## 已完结历史（按模块归档）

- [进度-全局框架.md](进度/进度-全局框架.md) — 全局框架 已完结进度（此模块未完结见上方看板）
- [进度-知识库.md](进度/进度-知识库.md) — 知识库（原编辑器）已完结进度（此模块未完结见上方看板）
- [进度-命令面板.md](进度/进度-命令面板.md) — 命令面板 已完结进度（此模块未完结见上方看板）
- [进度-图谱视图.md](进度/进度-图谱视图.md) — 图谱视图 已完结进度（此模块未完结见上方看板）
- [进度-设置.md](进度/进度-设置.md) — 设置 已完结进度（此模块未完结见上方看板）
- [进度-AI问答.md](进度/进度-AI问答.md) — AI 问答 已完结进度（此模块未完结见上方看板）
- [进度-平台规划.md](进度/进度-平台规划.md) — 平台规划 已完结进度（此模块未完结见上方看板）

### 已完结历史（插件）

> 插件框架/平台规范功能归档在 `进度/进度-插件.md`；**每个插件**的功能各自归档到按插件命名的独立子文件。

- [进度-插件.md](进度/进度-插件.md) — 插件框架/规范（市场、目录机制、Ribbon/右键/命令面板、编辑器能力、快捷键、版本同步；含已移除的「内容高亮」历史记录）
- [进度-插件-MarkdownEditor.md](进度/进度-插件-MarkdownEditor.md) — Markdown Editor 插件
- [进度-插件-MinimalTheme.md](进度/进度-插件-MinimalTheme.md) — Minimal Theme 插件（PL-18、PL-21）
- [进度-插件-CodeHighlight.md](进度/进度-插件-CodeHighlight.md) — Code Highlight 插件（PL-17）

## 常用命令速查（在 second-brain 内）

| 操作 | 命令 |
| ---- | ---- |
| 启动桌面版 | npm start 或双击 start-desktop.bat |
| 网页版调试 | 双击 start-server.bat，访问 http://127.0.0.1:8000/index.html |
| 打包 exe | npm run dist（产物在 second-brain/dist/） |

***

## 各模块需求最大编号

> 与 `doc/功能需求文档.md` 编号表同步。登记新需求前先查本表取下一编号，避免编号冲突。
> 规则：每个模块/前缀取**已用至**最大编号，新需求在其后继加 1。

| 模块 | 前缀 | 已用至 | 下一个 |
| ---- | ---- | ---- | ---- |
| 全局框架 | G- | 16 | G-17 |
| 知识库（原主编辑器） | ED- | 45 | ED-46 |
| 命令面板 | CMD- | 12 | CMD-13 |
| 图谱视图 | GR- | 18 | GR-19 |
| 插件市场 | PL- | 21 | PL-22 |
| 插件-MarkdownEditor | MD- | 08 | MD-09 |
| 设置 | ST- | 42 | ST-43 |
| AI 问答 | AI- | 24 | AI-25 |
| 平台规划 | SEC- / ENC- / GIT- | 02 / 01 / 14 | SEC-03 / ENC-02 / GIT-15 |

***

## 架构重构记录

> 架构 / 工程结构 / 文档结构的历史重构记录已独立成文，见 **`doc/架构重构记录.md`**。
> 此处不再保留正文，新增重构记录直接追加至该文件。
