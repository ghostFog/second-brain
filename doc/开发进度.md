# 第二脑 · 开发进度跟踪表

> 用途：按模块跟踪每个功能点状态。
> 状态取值：计划 -> 设计 -> 编码 -> 开发完成 -> 测试完成 -> 完成。
> 维护约定：**已完结历史已归档到 `进度/进度-<模块>.md`，本文件只保留未完结条目**与模块导航。
> 新增功能点：先登记到下方「未完结看板」，完成后再移入对应模块子文件。

***

## 未完结进度（看板）

| 模块 | 编号 | 功能点 | 状态 | 记录 |
| ---- | ---- | ---- | ---- | ---- |
| 编辑界面 | ED-10 | 更多操作 | 计划 | 占位，待实现 |
| 编辑界面 | ED-21 | 标签云 | 开发完成 | 点击筛选占位 |
| 编辑界面 | ED-54 | 精简编辑页签栏与编辑工具栏 | 开发完成 | 精简 tab 栏右侧按钮区：删除「删除笔记」「显示/隐藏行号」（该行号切换无实际功能，设置-编辑器「显示行号」一并移除）「自动换行」按钮与「编辑 · 即时渲染」描述标签；「宽屏」「表格列宽」「全屏」三按钮迁入 vditor 编辑工具栏（editor-vditor VDTOOLBAR 新增 sb-wide/sb-table-layout/sb-fullscreen，宽屏/列宽经 window.mdeToggleWide/mdeToggleTableAuto 桥接插件能力，宿主新增 window.vdSetToolbarCurrent 供插件同步其高亮，插件 markdown-editor 停止 #ed-plugin-wide-slot 注入改为同步工具栏高亮）。npm test 247 通过，待重启应用真机验证 |
| 编辑界面 | ED-55 | 全屏交互优化 | 开发完成 | 全屏/退出不再弹右上角「退出全屏」浮动按钮，退出靠 Esc 或再点 vditor 工具栏「全屏」按钮；切换全屏后若大纲浮层正显示，经 sbOutlinePosition 重定位到新的编辑区右上角。npm test 247 通过，待重启应用真机验证 |
| 编辑界面 | ED-56 | 编辑工具栏由插件注入 | 开发完成 | 「宽屏」「表格列宽」「大纲」三能力归位 markdown-editor 插件：按钮经宿主注入桥 sbVdToolbarAdd 注入编辑工具栏（宿主新增 __sbVdToolbarInjects 队列 + 注入 API，app-plugins 建立、editor-vditor 构建工具栏时并入 VDTOOLBAR）；大纲浮层 UI（sb-outline-*）迁入插件 main.js，宿主仅留最小桥（sbOutlineSource 取源 / vdSetToolbarCurrent 高亮 / sbOutlineChange 事件刷新）；大纲 CSS 迁入 plugins/markdown-editor/styles.css；「全屏」仍宿主自接管、用同桥注册。editor-vditor 删 VDTOOLBAR 三自定义项并迁删 sb-outline-* 逻辑，input/切文档/全屏改派发 sbOutlineChange。npm test 241 通过，待重启应用真机验证 |
| 编辑界面 | ED-57 | sv 分屏源码显示行号 | 开发完成 | vditor lineNumber 只服务代码块、不侧 sv 源码 textarea，故宿主自绘 sv 源码行号列：新增 vdSvGutterRefresh + .sb-sv-gutter（贴源区左缘、随 ta.scrollTop 平移 translateY、按行数重建、textare paddingLeft 让位），接入 applyVdVisibility/input，非 sv 自动清理；editor-vditor.js + css/app.css 新增，regression.test.js 补 vdSvGutterRefresh 暴露与无实例冒烟断言。npm test 243 通过，待重启应用真机验证。**后续修复**：①行号换行由「canvas 像素宽 ÷ 可用宽向上取整」估算（西文按词换行高估、窄分屏逐行下漂对不齐）改为隐藏测量元素复刻 textarea `pre-wrap + break-word` 真实断行的 `vdSvLineRows`（替换 `vdSvTextWidth`），各 sv 布局（分屏/仅源码）行号与文本逐行对齐；②预览/阅读顶部 vditor 动作条（Desktop/Tablet/Mobile/Wechat/知乎 一排按钮）置空 `preview.actions:[]` 整个隐藏；③预览/阅读模式代码块行号（vditor 内置 `.vditor-linenumber`，绝对定位不参与 pre 高度、超区溢出且不随滚动条同步）判定不可修，直接关闭 `preview.hljs.lineNumber:false` 并删除 app.css 中针对不存在 `.hljs-ln` 结构的死代码；④纯预览（vdPreview='preview'）时 sv 源码 textarea 被 display:none，但 gutter 在 vdMode==='sv' 时不会被清理、残留渲染到预览布局且行高测量异常（截图 1/空/2,3/空/4,5 不规则），`vdSvGutterRefresh` 隐藏判定增加 `getComputedStyle(ta).display==='none'` 一并移除 gutter 并还原 paddingLeft，源码模式不受影响。npm test 243 通过 |
| 编辑界面 | ED-53 | 系统「打开方式」打开文件按临时处理 | 开发完成 | OS 右键→打开方式→第二脑传入外部文件按临时文件打开：main.js 新增 `pickOpenWithArg`（从 process.argv 首次启动 / `second-instance` commandLine 检出存在的 .md/.markdown/.txt 绝对路径）+ `forwardOpenFile`（待窗口就绪后经 `open-file` 事件推送）+ `openWithFile`（聚焦窗口后推送）；preload 新增 `noteDesktop.onOpenFile`；渲染端 project-mode `sbTempFiles.init` 订阅 `open-file` → `openTemp(abs)`（readFileExternal 任意路径可读 + recordRecent 登记可写，可编辑写回）。npm test 243 通过，待重启应用真机验证（右键菜单出现「第二脑」需打包注册 fileAssociation）；追加「清除最近打开」：库下拉「最近打开」悬浮二级菜单最末新增「清除最近打开」项（app-vault.js 新增 data-vault-act=clear-temp 分支 + clearTempRecent 调 `temp:recentClear`），main.js 新增 `temp:recentClear`（recentTemp 置空 + persistRecentTemp）、preload `tempRecent.clear`；列表为空时不显示二级菜单逻辑保持不变。npm test 247 通过 |
| 全局框架 | G-17 | 打开项目（非知识库工作区） | 开发完成 | `js/project-mode.js` sbProject + main.js `winProjects`/project:* IPC + preload `project.*`：库下拉「打开项目…」选目录 → 独立项目窗口（复用知识库文件树/编辑器/页签，相对 rel 路径直连 project:read/save、Git 状态着色写 `__gsColoring`）；已打开项目列库下拉「一般项目」分组（知识库仍在「其他知识库」），点击经 `project:openPath` 打开/聚焦；托盘「一般项目」二级展开；项目窗口隐藏 graph/plugins/ai ribbon 按钮 + 右侧侧边面板 + 底部统计，不建笔记数据/向量索引；默认打开最后一个 Git 变更文件。npm test 240 通过，待重启应用真机验证；修复「打开项目报 n.folder.split is not a function」：walkProject 的 folder 为布尔 isFolder 标记（目录=true/文件=false），与 renderFileTree 期望的「父目录路径字符串」语义不符，project-mode buildTreeData 改为 `isFolder=it.folder===true` + `folder=父目录路径`；renderFileTree 对 folder 加 `typeof string` 过滤空串（`''.split('/')` 得 `['']` 会造出 key=''、parent='' 自引用目录节点致无限递归）。npm test 247 通过 |
| 全局框架 | G-18 | Git 失败日志与日志目录可配置 | 开发完成 | ①主进程 `gitRun` 统一执行点在任务失败（exit 非 0）时 `appendLog` 记录【级别/git 命令/stderr/cwd】（project:gitStatus 复用同一 gitRun 自动覆盖）；②运行日志**目录可配置**（设置中只存目录不拼文件名）：`logBaseDir()` 返回配置目录（默认 userData/logs）承载两份日志，普通日志 `app.log`、**错误日志 `error.log` 单独落盘**（error/fatal 走错误文件）；新增 `app:getLogDir`/`app:setLogDir`/`app:readLog` IPC（readLog 按 info/error 取末 500 行）+ preload `getLogDir`/`setLogDir`/`readLog`，`loadConfig/saveConfig` 增 `logDir` 字段持久化 settings.json，重启保持；③设置「常规-其他」新增「运行日志目录」输入框 + 「浏览…」（`app:chooseLogDir` 系统目录选择器，选中填入待保存）+「保存目录」+「查看」（`bindLogDir` 初值回填当前目录，`bindActionButtons` 增 set-logdir/pick-logdir/view-log 分支写回主进程）；④**日志查看独立浮窗**：桌面版「查看」改经 preload `openLogViewer` 打开 main.js 新建的 frameless 浮窗（`logview.html`，`app:openLogViewer` 打开/聚焦、`app:closeLogViewer` 关闭，自绘标题栏 `-webkit-app-region:drag` 可拖出主窗口；浮窗内切换「运行日志/错误日志」、关键字筛选 input 实时过滤、「实时」开关 1.5s 轮询 + 自动滚底、**清屏**（清界面不动文件）、**清理**（`app:clearLogFile` 清空当前日志文件内容）、最小化/关闭）；网页模式回退旧 DOM `viewLogModal`。**增强**：单日志文件大小可配置——`LOG_MAX_BYTES` 常量改为 `logMaxMB` 变量（默认 5，`writeLogLine` 阈值改为 `logMaxMB*1024*1024`，超限仍自动拆分 `.1`），`loadConfig/saveConfig` 持久化 `logMaxMB`，新增 `app:getLogMaxMB`/`app:setLogMaxMB`（校验 1-1024）/`app:clearLogFile` IPC + preload `getLogMaxMB`/`setLogMaxMB`/`clearLogFile`，设置页「常规-其他」新增「单日志文件大小(MB)」输入框 +「保存」（set-logmax 分支写回）；`bindLogDir` 同步回填大小。方案文档 design/documents/日志浮窗增强-清屏-清理-大小可配置.md。node --check 语法通过；npm test 240 通过，待重启应用真机验证 |
| 插件 | MD-04 | 链接/反向链接数据管理 | 开发完成 | 前端消费主进程链接索引：renderBacklinks 改用 fileMeta(backlinks)、buildGraph 改用 linksIndex(outlinks)、编辑区内链接点击按内部/外链分流（新开页签）；相对路径按源笔记路径解析归一（./ ../ 同目录 / 开头从库根；wiki 纯名保持全局匹配）；新增 notes:linksIndex IPC |
| 命令面板 | CMD-04 | 打开笔记 | 开发完成 | 入口展示 |
| 命令面板 | CMD-05 | 搜索笔记 | 开发完成 | 入口展示 |
| 命令面板 | CMD-06 | 导出为 PDF | 设计 | 待实现 |
| 命令面板 | CMD-10 | 同步设置 | 设计 | 待实现 |
| 命令面板 | CMD-12 | 结果计数 | 开发完成 | 随过滤更新 |
| 图谱视图 | GR-10 | 脉冲动画 | 开发完成 | CSS 装饰 |
| 图谱视图 | GR-11 | 图例 | 开发完成 | 渲染 |
| 图谱视图 | GR-12 | 操作提示 | 开发完成 | 渲染 |
| 图谱视图 | GR-13 | 面板标题 | 开发完成 | 渲染 |
| 图谱视图 | GR-15 | 基本信息 | 开发完成 | 渲染 |
| 图谱视图 | GR-16 | 标签 | 开发完成 | 渲染 |
| 插件 | PL-07 | 量化指标 | 开发完成 | 渲染 |
| 设置 | ST-21 | Web 服务开关 | 计划 | 新增需求，待实现 |
| 设置 | ST-22 | Web 服务选择 | 计划 | 新增需求，待实现 |
| 设置 | ST-23 | Web AI 问答 | 计划 | 新增需求，待实现 |
| 设置 | ST-40 | 可用模型实时列表（后台轮询 + 全局缓存） | 开发完成 | ①引擎新增可用模型**全局缓存** `_available`：`refreshAvailableModels()` 按已配置 baseUrl 去重，Ollama 走 /api/tags、OpenAI 兼容走 /models（`listOpenAiModels`），失败标记 error；`startModelPolling` 改为同时刷新运行状态 + 可用模型，**由 main.js 在 initAiEngine（配置加载完成后）调用**（修复原启动时 cfg.models 未加载空跑导致设置页打开几秒后才有状态）；`getAvailableModels()` + `ai:availableModels` IPC + preload 桥接；②**设置-AI「可用模型（实时）」区块已移除**（用户反馈不需要：settings-panels 删除 #ai-avail-models/#ai-avail-ts，settings-ai 删除 renderAvailableModels 及其在 startAutoRefresh/tick/钩子中的调用，设置页仅保留生成模型列表运行状态刷新）；③AI 问答页模型下拉**显示「已加载到内存（运行中）」的模型**：配置运行中（listModels running=true，本地 Ollama 仅运行中/远程 OpenAI 始终可用）立即渲染 + 经 `getRunningMap()` 运行状态缓存合并**未配置但已加载**的模型（`avail:<baseUrl>:ollama:<model>` 临时 id，标题标「已加载到内存」，模型卸载即从下拉移除；不再合并服务端 tags 全部可用模型），每 5 秒 `__aiModelSyncTimer` 同步（生成中不刷新）；④ask 链路支持 `modelId`：app-ai `ai.ask(q, carry, agentId, modelId)` → preload → main ai:ask → engine.ask，`_modelById` 对 avail id 从 _available 构造临时模型（apiKey 取同 baseUrl 已配置项）。npm test 196 通过（ST-40 断言更新：设置页断言改为确认可用模型区块已移除），待重启应用真机验证 |
| 设置 | ST-41 | 模型数据钩子实时刷新（类 Vue 响应式） | 开发完成 | 引擎新增 `onModelsUpdated(cb)`/`_notifyModelsUpdated()`（回调数组 `_modelCbs`，异常隔离），`startModelPolling` 的 run 改 `Promise.all([refreshRunningMap, refreshAvailableModels]).then(通知)`（每次轮询/手动刷新完成触发钩子）；main.js `initAiEngine` 注册 `aiEngine.onModelsUpdated` 回调 → 向所有窗口推送 `ai:models-updated` 事件；preload 暴露 `onModelsUpdated(cb)`（removeAllListeners + on 监听）；设置-AI `startAutoRefresh` 与 AI 问答页 `refreshOnce` 均监听该钩子**立即重绘/重刷下拉**（5 秒定时器保留兜底）——数据变化即显示，不等定时器；同时三处 fetch 查询超时 `AbortSignal.timeout(10000)`→`3000`（Ollama /api/tags、/api/ps、OpenAI /models），服务不可达 3 秒内快速失败并显示具体 error（消除「打开设置/AI 问答约 10 秒才显示状态」）。npm test 196 通过（ST-41 新增 5 条断言），待重启应用真机验证实时刷新与 fetch failed 排查 |
| AI 问答 | AI-17 | AI 创建笔记 | 计划 | 新增需求，待实现 |
| 平台规划 | SEC-01 | 启动密码 | 开发完成 | 首次不强设，用户在「设置→隐私与安全→应用密码」开启；main.js 新增安全区块（securityFile 存 userData/security.json，Node crypto.scrypt+randomBytes 生成 salt+timingSafeEqual 校验，app:setPassword/unlock/getSecurity/setLockConfig IPC），preload 暴露 noteDesktop.security.*；index.html 新增 #lock-screen 遮罩 + 新文件 js/app-security.js（启动时 hasPwd&&!unlocked 则 body.locked 隐藏主视图并弹遮罩，解锁恢复）；settings 隐私区块新增应用密码 UI（设置/修改/移除+超时锁定开关与分钟）与 bindSecurity。设计见 design/documents/应用安全-启动密码与超时锁定.md。npm test 241 通过，待真机验证 |
| 平台规划 | SEC-02 | 超时锁定遮罩 | 开发完成 | 桌面版主进程 powerMonitor 空闲轮询（SEC_POLL_MS=15s，达 lockMinutes*60 秒广播 app:locked，渲染端弹遮罩）；网页版无主进程时由 js/app-security.js 本地事件计时兜底；解锁经 app:unlock 校验成功后移除遮罩恢复原界面不动主视图。设计见 design/documents/应用安全-启动密码与超时锁定.md。npm test 241 通过，待真机验证 |
| 平台规划 | ENC-02 | 笔记落盘加密（按库） | 开发完成 | 笔记内容落盘加密 + 读取解密：main.js 新增「笔记加密」模块（组合算法 comboKdf=SHA-256 链式迭代+permuteBytes 字节置换 + AES-256-CTR + comboMask XOR 掩码流，非单一公开算法；笔记内容 AES-256-GCM 落盘 `ENC1:` 头）。**按库设置**：配置存 `<库根>/.second-brain/vault-enc.json`（.gitignore 已忽略，不进 git），`app:encGetState`/`app:encSetPassword` IPC + preload `enc.*`；**盐规则**：未设应用密码用固定盐 `shr25.com`，设置应用密码后以其明文作盐（unlock 时进内存），取消/修改应用密码时 reEncryptAllVaults 按旧盐→新盐重加密各库密码配置还原/切换盐；**懒加载**：解密后笔记加密密码/派生密钥仅内存缓存（encKeyCache），应用启动不预载、首次读写才 ensureNoteKey 解密；**全链路联动**：notes:read/save/create/delete、ai:previewChunk、walkDirMeta 元数据扫描（加密库跳过 stripMetaId 写回避免破坏密文）走 vaultReadText/vaultWriteText 解密/加密读写，AI 引擎注入 _vaultReadSync 使索引读解密内容；设置页「隐私与安全」新增「笔记加密」区块（enc-status 状态徽标 + enc-set/enc-change/enc-remove 三按钮 + enc-salt 盐来源显示）与 bindEncryption。npm test 243 通过，待重启应用真机验证。**ENC-03 增强（库根加密状态文件+打开解锁）**：加密配置文件迁至**库根隐藏文件 `.vault-enc.json`**（记录本库是否已启动加密，**纳入 git 管理随仓库同步**；旧 `.second-brain/vault-enc.json` 由 encReadConfig 读取兼容+自动迁移删除，设置/取消密码时清理旧文件）；新增 `encNeedsUnlock`（已加密且内存无缓存密钥）+ `app:encVerify` IPC（输入笔记加密密码 comboDecrypt 解密密文比对，**验证通过才缓存派生密钥**）+ getState 返回 `locked`；preload `enc.verify`；打开库（启动/切换/新建窗口）检测 locked 时渲染端弹 `#enc-unlock-screen` 遮罩（独立 `body.enc-locked` 隐藏主视图，与应用锁屏解耦）输入密码验证，输对进入、输错提示。node --check 通过；npm test 243 通过，待重启应用真机验证。**架构重构（真正笔记密钥 K+双文件）**：K=deriveNoteKey(输入笔记密码)（组合算法转固定 32 字节）；库根 `.vault-enc.json` 只存 proof（encMakeProof：K 加密固定哨兵的确定性固定值，进 git）；`.second-brain/vault-enc.json` 存 K 经 encSaltStr 组合加密密文（encWriteKeyCipher，每库独立）；ensureNoteKey 用盐解出 K（encReadKeyCipher→encB64ToKey→32B 校验）；encNeedsUnlock=有 proof 且无缓存 K；app:encVerify=输入密码→derive K→encVerifyProof 校验→通过缓存 K+落盘 .second-brain；app:encSetPassword 设 proof+K密文、取消校验旧密码后 encRemoveFiles；reEncryptAllVaults 只重加密 K 密文。enc-selftest 12 项（往返/固定值/错盐/确定性）通过后删。npm test 243 通过。**双密码重构（M1/M2 + 已加密文件列表 + M1_1 事务）**：M1=用户密码（锁屏盐，明文仅内存 encAppPwdPlain 不落盘）；M2=deriveNoteKey(I1)（同输入同结果 32B）；库根 `.vault-enc.json` 改存 M2 加密的**已加密文件列表**（`{v:2, list: encryptNoteText(JSON{files})}`，相对库根路径；删 encMakeProof/encVerifyProof/ENC_PROOF；解密列表成功=密码正确+支持部分加密）；新增 normRel/encListCipher/encListDecrypt/encWriteListCipherSync/encLoadList/encIsFileEncrypted/encListAdd/encListRemove/encAllNotePaths/encEncryptAll（每 50 文件落盘一次）/encReencryptAll/encDecryptAll/encRebuildList（目录移动/重命名/删除后按磁盘 ENC1: 头重建）；读写链路 vaultRead*/vaultWrite*/notes:delete/moveDir/renameDir 按列表判断（在列表=加解密，不在=明文）；`.second-brain/vault-enc.json` 存 M3=comboEncrypt(M2b64, 盐)；打开库解锁=encNeedsUnlock→tryAutoNoteKey 自动解锁（盐解 M3+列表校验）失败才 app:encVerify 输入 I1 重派生校验并落盘 M3；M1 修改/取消事务化：tempPwd(M1_1) 先落盘 security.json（取消标记 ENC_TEMP_NO_PWD='-----notPwd------'）→ reEncryptAllVaults(旧盐, M1_1/固定盐) 重算各库 M3 → 成功才落盘 M1/删 M1 → 清 tempPwd；loadSecurity 改 async 启动恢复未完成事务；取消笔记密码=encDecryptAll 全库解密回明文+encRemoveFiles；preload enc.* 与设置页文案同步（取消密码时已加密笔记解密还原为明文）。node --check 通过；npm test 243 通过；enc-roundtrip 临时自测 21 项（M2 确定性/AES-GCM 往返/列表往返/盐切换/重算 M3 保 M2）通过后删。**ENC-04 增强（多密钥 keyring + 列表 key 字段）**：`.second-brain/vault-enc.json` 升级 M3 **v:2 多 key**（`{v:2, keys:{keyId: comboEncrypt(b64(M2),盐)}}`，一个库可存多个密码的多个 M2，兼容 v:1 单 cipher 读入自动迁移）；新增 `encKeyId`（SHA-256(base64(M2)) 前 16 位 hex，**密码的确定性指纹**，同密码跨设备一致，与笔记 id 无关——换设备无 M3 时输入任意正确密码 I1 即可重建 keyring）；`encReadKeyring`/`encWriteKeyCipher(root, ring)` 读写 keyring；库根列表升级 **v:3**（`files:[{rel, key}]`，每条记该文件加密所用 keyId，兼容 v:2 纯路径）；缓存改 keyring 语义：`encKeyCache`=root→Map<keyId,M2>、新增 `encMainKeyCache`（主 key）、`encListCache`=root→Map<rel,keyId>；`tryAutoNoteKey` 逐 key 解列表定主 key、`encMainKey`/`ensureNoteKey` 取主 key；`decryptNoteTextAny(raw, ring, preferKeyId)` 解密容错（条目 key 优先→遍历 keyring→全失败返原文）；读写链路 vaultReadTextByRoot/Sync/Text 按条目 key 降级解密；`encReencryptAll(root, oldRing, newKey)` 旧 keyring 解新 key 加密、条目 key 更新；`encEncryptAll`/`encListAdd` 条目记 `encKeyId(key)`；`encRebuildList` 逐 key 解文件定 keyId；`app:encVerify` 手动解锁把正确 key 并入 keyring 落盘；`app:encSetPassword` 设置=单 key 写入、修改=旧 key 保留+新 key 并入、取消=keyring 解密回明文；`reEncryptAllVaults` 逐 key 旧盐解新盐重算（v:1 迁移指纹 keyId）。node --check 通过；npm test 243 通过；enc-selftest 15 项（keyId 确定性/M3 多 key/修改密码旧 key 保留/取消容错/换设备重建/盐切换重算）通过后删。**Bug-066 修复（锁屏未解锁盐未定误判 locked）**：启动阶段锁屏未解锁、`encAppPwdPlain=null`（盐=固定盐）时 `app:encGetState` 用固定盐解 M3 失败误判 locked → 弹笔记密码遮罩，锁屏解锁后不重检。修复：`app:encGetState` 在 `secPwdHash && !appUnlocked` 时 locked 返回 false；`initVaultEncUnlock` 检测重入化并暴露 `window.SBEncUnlock.recheck`；`app-security.js` `doUnlock` 解锁成功后调 `recheck()` 重新判定（盐=应用密码明文）。node --check 通过；npm test 243 通过（待真机验证）。**Bug-067 修复（单篇笔记显示密文缺修复入口）**：该文件用旧密码 key 加密、当前 keyring 只有新 key（列表条目 key 不一致）时 `decryptNoteTextAny` 全失败返回密文原文且无修复入口。修复：main.js 新增 `encRepairNote(root, rel, pwd)`（输入旧密码 I1 派生 M2_test 试解，成功用当前主密钥重加密落盘 + 并入 keyring 落盘 M3 + 列表条目 keyId 强制更新为当前主 key）+ `app:encRepairNote` IPC；preload `enc.repairNote`；index.html 新增 `#enc-repair-screen` 遮罩；app-vault.js `initEncRepair`（事件只绑一次，暴露 `window.SBEncRepair.tryRepair`，成功清 edOutdated 后重新 openNote）+ app-layout.js 调用；editor-host.js `openNote` 检测 `c.startsWith('ENC1:')` 弹遮罩。node --check 通过；npm test 243 通过；临时自测 9 项断言全过后删（待真机验证）。**时序回归修复（锁屏未解锁后台提前弹修复框）**：锁屏未解锁盐未定，固定盐解不开 M3 时 openNote 误判「旧密码密文」提前弹 #enc-repair-screen 与锁屏遮罩叠层。修复：editor-host.js 密文检测改为先查锁屏状态（`nd.security.getState()` → hasPwd && !unlocked 则不弹等待解锁）；app-security.js doUnlock 解锁成功后新增重载当前笔记（edCurrent 内容以 ENC1: 开头则 reloadNote）——盐定后自动解密，仍解不开才弹修复框（锁屏密码输入正确后才开始）。node --check 通过；npm test 243 通过（待真机验证）。 |
| 平台规划 | ENC-01 | 加密/解密文件 | 计划 | 新增需求，依赖 PL-10/11/12 |
| 平台规划 | GIT-02 | 自动提交 | 开发完成 | Git Sync 插件：定时器按间隔自动 add+commit（默认 10 分钟、可在设置弹框调整）；可开关。npm test 159 通过，待手动交互验证 |
| 平台规划 | GIT-03 | 远程仓库配置 | 开发完成 | Git Sync 插件：设置弹框可设置/更新 origin 地址。**增强**：目录区/空白区右键 Git 子菜单新增「远程路径」（`gs-ctx-dir-remote`/`gs-ctx-repo-remote`，图标 git-compare-arrows），点开**弹框**（openModal，非 prompt，Electron 渲染进程不支持 prompt）展示当前 origin 为默认值、输入后保存（复用 saveRemote 增/改 origin）；另修文件树右键二级子菜单超可视区——父菜单靠右/靠底时加 `.ctx-flip-sub`/`.ctx-grow-up` 类使子菜单向左/向上展开（file-tree-ctx.js + app.css）。npm test 240 通过，待重启应用真机验证右键入口、弹框保存远程地址、菜单不超视口 |
| 平台规划 | GIT-04 | 推送/拉取 | 开发完成 | Git Sync 插件：「立即同步（提交+推送）」与设置弹框「拉取」；自动推送为可配开关（默认关）。npm test 159 通过，待手动交互验证 |
| 平台规划 | GIT-16 | Git 稳定性：锁冲突自愈+无远程探测静音 | 开发完成 | Bug-062：主进程 gitRun 改为**按 cwd 串行队列执行**（GIT_QUEUES/gitEnqueue），同仓库 git 命令排队防并发占锁；新增 gitLockConflict 判定 + removeStaleGitLocks 清 `.git/*.lock` 残留，gitExecOnce 遇锁冲突清理后重试一次（cannot lock ref 'HEAD' 自愈）；git:check 探测类命令（status/rev-parse/remote get-url）传 `logFailure:false`，无远程不再写「git 操作失败」错误日志，真正 git 操作失败仍照常记录。Bug-063 修正：removeStaleGitLocks 清理正则由 `/\.[0-9a-zA-Z-]+\.lock$/`（不匹配单点 `master.lock`/`HEAD.lock`，自愈失效）改为 `/\.lock$/`；另修 git-sync 右键「对比」图标 compare 缺失（改 `git-compare-arrows`，lucide 真实键 GitCompareArrows）。node --check 语法通过；npm test 240 通过；锁正则以 `master.lock/HEAD.lock/packed-refs.lock` 实测命中，待重启应用真机验证 |
| 平台规划 | GIT-05 | 查看/还原版本 | 开发完成 | Git Sync 插件：命令「查看版本历史」弹框列最近版本 + Diff/还原到此；文件树右键「查看版本历史」；还原用 git checkout 覆盖工作区，不动分支/历史。npm test 159 通过 + 临时目录验证 checkout 语义，待手动交互验证 |
| 平台规划 | GIT-06 | 同步到仓库（自动/手动） | 开发完成 | 见 GIT-02/04 记录；自动推送开关（默认仅自动提交） |
| 平台规划 | GIT-07 | 从 Git 打开知识库 | 开发完成 | Git Sync 插件：命令「从 Git 打开知识库…」输入 URL → host 选目录克隆为独立库并新开窗口；临时目录验证 clone。npm test 159 通过，待手动交互验证 |
| 平台规划 | GIT-08 | 宿主 Git 桥接（白名单） | 开发完成 | main.js 新增 git:run/git:check、vault:cloneGit IPC + preload noteDesktop.git/cloneGit；子命令白名单 + cwd 锁定知识库根，execFile 非 shell 规避注入。npm test 159 通过 |
| 平台规划 | GIT-10 | 单文件历史/还原 | 开发完成 | fileLogModal 改左右布局：左列仅 hash+时间+「还原」，点击在右列对比框展示该版本 diff，顶部滑动开关（开=对比本地 / 关=对比上一版本），弹框加大；删除独立「查看」按钮；右键菜单仅留「查看此文件历史」；host 在 action 执行前恢复 __pluginCtxNote 修复右键取不到目标笔记。回滚：还原/回滚成功后宿主 editor.reloadNote 刷新编辑器到磁盘、onEdInput 仅在内容变化且非脏时写盘（Bug-055，待手动验证）。npm test 163 通过，待手动交互验证 |
| 平台规划 | GIT-11 | 还原彻底性 | 开发完成 | 整库还原弹框带「同时清理未跟踪文件/空目录」复选 → git clean -fd（host 白名单新增 clean）；临时目录验证 clean -fd 移除 untracked+空目录、保留 staged/已跟踪。npm test 163 通过 |
| 平台规划 | GIT-15 | 过滤设置（编辑 .gitignore） | 开发完成 | Git Sync 设置弹框新增「编辑 .gitignore」：读库根 .gitignore 到文本域、保存写回（notes:read/save，manifest 加 vault.write）；git add . 天然尊重 .gitignore，忽略路径不进自动提交。npm test 163 通过，待手动交互验证 |
| 平台规划 | GIT-16 | 实时提交 & Ribbon 同步 | 开发完成 | 设置弹框新增「实时提交」开关（文件变动即提交本地）；经 #file-tree MutationObserver 防抖触发静默 commitNow(false)。Ribbon 按钮改为同步：宿主 git-sync ribbon 加 actionKey=git-sync → syncNow（未初始化 confirm→initRepo；已初始化→commitNow(autoPush)）。GIT-13 已取消，状态反馈收敛到目录树即时着色（文件重绘触发生成着色/调色）。npm test 163 通过，待手动交互验证 |
| 平台规划 | GIT-19 | 初始化创建默认过滤文件（.gitignore） | 开发完成 | initRepo 在 git init 成功后读取库根 .gitignore（notes:read，不存在抛错）→ 不存在则写默认忽略规则（notes:save，DEFAULT_GITIGNORE：忽略 .second-brain/.session/.obsidian/.trash 与临时/日志/系统文件），已存在则保留用户内容；.resources/（上传资源文件）需版本管理，不忽略。npm test 163 通过，待手动交互验证 |
| 插件(知识库) | MD-07 | 大纲移至 vditor 浮层 + 快捷键 | 测试完成 | ① 侧边面板移除大纲区块：`views/vault.html` 删除 ed-outline 区块、`editor-host.js` renderArticle 移除大纲渲染、插件/宿主 sidebar 注册移除 outline 项；② Markdown Editor 插件新增命令「显示大纲」(`mde-outline`，`manifest.json` commands 声明默认快捷键 `Ctrl+Shift+Q`)，经 `window.vdToggleOutline` 展开/收起大纲浮层；③ **浮层完善（点标题跳转 + 父级折叠/展开）**：弃用 vditor 内置 outline（其跳转依赖 `scrollTop/offsetTop` 的滚动容器在本应用多面板/transform 环境下对不上、点不动、折叠状态易被重渲染重置），在 `editor-vditor.js` 自研 `sb-outline-*` 浮层面板（挂 body，fixed 贴编辑区右侧），工具栏 `outline` 项替换为自定义 `sb-outline` 按钮；从当前可见编辑内容源（IR/wysiwyg 编辑节点、SV 分屏的预览面板）取标题构建层级树，点击标题用 `scrollIntoView` 跳转到正文（自动滚动到相应容器），父级标题带折叠箭头可收放子级，面板打开时随 vditor input 防抖重建；纯 SV 源码模式不可用（等价 native 禁用）；样式入 `css/app.css`。暴露 `__sbOutline`（text/tree/open）供断言。npm test 228 通过（含大纲 7 条断言），UI 待手动验证；④ **工具栏悬浮完善**：vditor 内置项悬浮用 `::after`+`aria-label` 渲染，原生 `title` 也在本应用 `#ed-vditor`(overflow:hidden) 容器内不可见、连续多次尝试未果；`sbEnsureToolbarHoverTips` 在 after 后为每个图标按钮写 `data-sb-tip`（名称+实际快捷键，`<键>` 转 `[键]`，无快捷键项仅名称，空 label 回退 data-type），并经**自绘悬浮浮层 `.sb-tip`** 显示——fixed 定位、document 级事件委托（按钮重建仍生效）、绝对可见（暴露 `__sbEnsureToolbarTips`）；并**移除 vditor 自带 `aria-label`/原生 `title`**，杜绝「查找/替换/大纲」等按钮 vditor 自带 ::after 悬浮与自绘浮层重复显示，统一使用自绘版；⑤ **上传悬浮不显示修复** + **大纲 resize 重定位**：上传按钮 vditor 单独渲染为 `div.vditor-tooltipped`（非 button，见其 toolbar 构建 `tagName = name==='upload'?'div':'button'`），自绘委托原只匹配 `button` 故上传永不悬浮，改为命中 `.vditor-tooltipped` 覆盖上传 div；并**CSS `!important` 屏蔽 vditor 原生 `::before/::after` 悬浮**（aria-label 会被 vditor 在 hover/重建时机重设，仅 removeAttribute 不够，导致与自绘浮层重复显示两个；上传/查找/替换/大纲均触发），统一只留自绘版；**悬停 file input 时清空 title** 屏蔽 Chromium 约 3 秒后出现的「未选择任何文件」UA 提示（vditor 用透明 file input 覆盖在按钮上承接原生选择，与上传点击无关，不影响功能）；大纲浮层打开后增加一次性 window resize 监听重定位（`position:fixed`，始终贴当前编辑区右侧上下居中，不再固定在首次打开位置），`__sbOutline` 暴露 `position`。npm test 240 通过（含悬浮/上传 12 条断言），UI 待手动验证 |

***

## 已完结历史（按模块归档）

- [进度-全局框架.md](进度/进度-全局框架.md) — 全局框架 已完结进度（此模块未完结见上方看板）
- [进度-编辑界面.md](进度/进度-编辑界面.md) — 编辑界面（原知识库·原编辑器）已完结进度（此模块未完结见上方看板）
- [进度-命令面板.md](进度/进度-命令面板.md) — 命令面板 已完结进度（此模块未完结见上方看板）
- [进度-图谱视图.md](进度/进度-图谱视图.md) — 图谱视图 已完结进度（此模块未完结见上方看板）
- [进度-设置.md](进度/进度-设置.md) — 设置 已完结进度（此模块未完结见上方看板）
- [进度-AI问答.md](进度/进度-AI问答.md) — AI 问答 已完结进度（此模块未完结见上方看板）
- [进度-平台规划.md](进度/进度-平台规划.md) — 平台规划 已完结进度（此模块未完结见上方看板）

### 已完结历史（插件）

> 插件框架/平台规范功能归档在 `进度/进度-插件.md`；**每个插件**的功能各自归档到按插件命名的独立子文件。

- [进度-插件.md](进度/进度-插件.md) — 插件框架/规范（市场、目录机制、Ribbon/右键/命令面板、编辑器能力、快捷键、版本同步；含已移除的「内容高亮」历史记录）
- [进度-插件-MarkdownEditor.md](进度/进度-插件-MarkdownEditor.md) — Markdown Editor 插件
- [进度-插件-MinimalTheme.md](进度/进度-插件-MinimalTheme.md) — Minimal Theme 插件（PL-18、PL-21）
- [进度-插件-CodeHighlight.md](进度/进度-插件-CodeHighlight.md) — Code Highlight 插件（PL-17）

## 常用命令速查（在 second-brain 内）

| 操作 | 命令 |
| ---- | ---- |
| 启动桌面版 | npm start 或双击 start-desktop.bat |
| 网页版调试 | 双击 start-server.bat，访问 http://127.0.0.1:8000/index.html |
| 打包 exe | npm run dist（产物在 second-brain/dist/） |

***

## 各模块需求最大编号

> 与 `doc/功能需求文档.md` 编号表同步。登记新需求前先查本表取下一编号，避免编号冲突。
> 规则：每个模块/前缀取**已用至**最大编号，新需求在其后继加 1。

| 模块 | 前缀 | 已用至 | 下一个 |
| ---- | ---- | ---- | ---- |
| 全局框架 | G- | 18 | G-19 |
| 编辑界面（原知识库·原主编辑器） | ED- | 48 | ED-49 |
| 命令面板 | CMD- | 12 | CMD-13 |
| 图谱视图 | GR- | 18 | GR-19 |
| 插件市场 | PL- | 21 | PL-22 |
| 插件-MarkdownEditor | MD- | 08 | MD-09 |
| 设置 | ST- | 44 | ST-45 |
| AI 问答 | AI- | 24 | AI-25 |
| 平台规划 | SEC- / ENC- / GIT- | 02 / 04 / 14 | SEC-03 / ENC-05 / GIT-15 |

***

## 架构重构记录

> 架构 / 工程结构 / 文档结构的历史重构记录已独立成文，见 **`doc/架构重构记录.md`**。
> 此处不再保留正文，新增重构记录直接追加至该文件。
