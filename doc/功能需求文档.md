# 第二脑 · 功能需求文档

> 依据 `design/pages/*.html` 提取，按页面枚举**全部功能点**（含交互、数据、快捷键）。
> 实现/变更任何功能前，先对照本文件 + `设计文档.md` 确认需求范围，避免遗漏。

***

## 1. 全局框架（所有页面共享）

| 编号   | 功能点          | 说明                                  | 交互 / 数据                              |
| ---- | ------------ | ----------------------------------- | ------------------------------------ |
| G-01 | 标题栏库名        | 显示「我的笔记库」                           | 展示                                   |
| G-02 | 面包屑导航        | 如 日记/2024年/09月，末级高亮                 | 随当前笔记路径展示                            |
| G-03 | 命令面板入口       | 标题栏右侧「搜索 ⌘P」按钮                      | 点击或 `Ctrl+P/Cmd+P` 打开命令面板            |
| G-04 | 窗口控制         | 最小化 / 最大化 / 关闭（桌面版）                 | 自绘按钮，点击控制窗口                          |
| G-05 | Ribbon 导航    | 编辑器 / 搜索 / 图谱 / 插件 / 设置             | 点击切换视图；选中项高亮                         |
| G-08 | 笔记右键菜单       | 文件树笔记行右键                            | 在资源管理器中显示该笔记（桌面版）                    |
| G-09 | 库历史列表 / 拖拽调宽 | 库下拉框记录打开过的库、点击切换、悬浮 × 删除；目录区拖拽调宽    | 桌面版持久化到 settings.json / localStorage |
| G-06 | 同步状态点        | Ribbon 底部圆点                         | 显示同步状态（绿=已同步）                        |
| G-07 | 笔记库选择/切换     | 标题栏库名点击展开下拉框                        | 选择目录打开/切换库、恢复默认、持久化                  |
| G-10 | 迁移默认知识库      | 把默认知识库（userData/vault）内容迁移到用户选择的新目录 | 移动语义：选目录→二次确认→复制→清空原库→新目录成为默认库并持久化   |

***

## 2. 主编辑器（editor）

### 2.1 左侧文件树（240px）

| 编号    | 功能点      | 说明                         | 交互                 |
| ----- | -------- | -------------------------- | ------------------ |
| ED-01 | 文件搜索     | 占位“搜索文件…”                  | 输入过滤文件树            |
| ED-02 | 新建笔记     | `+` 按钮                     | 点击新建笔记             |
| ED-03 | 文件夹展开/折叠 | 带箭头切换（如 日记/2024年/09月）      | 点击箭头折叠/展开子树        |
| ED-04 | 文件树空白区右键 | 空白处右键弹菜单                   | 新建笔记 / 新建目录（桌面版写盘） |
| ED-04 | 文件夹计数徽标  | 如 笔记 12、模板 5、附件 28         | 展示                 |
| ED-05 | 文件行与相对日期 | 如 09-01.md · 今天 / 昨天 / 2天前 | 展示；点击行选中并打开        |
| ED-06 | 文件选中态    | 品牌色底 + 白字                  | 点击文件高亮             |
| ED-07 | 库统计      | 共 47 篇笔记 · 2.4 MB          | 展示                 |

### 2.2 编辑器区

| 编号    | 功能点         | 说明                                                         | 交互                                         |
| ----- | ----------- | ---------------------------------------------------------- | ------------------------------------------ |
| ED-08 | 多标签页        | 每个文件一个 Tab，可横向滚动                                           | 点击切换；点 × 关闭                                |
| ED-09 | 编辑/预览模式     | 行尾 `pencil` / `eye` 切换                                     | 点击切换模式                                     |
| ED-10 | 更多操作        | `more-horizontal` 按钮                                       | 点击弹出更多菜单（占位）                               |
| ED-11 | 文件元信息       | 日期 / 最后编辑 / 字数                                             | 展示                                         |
| ED-12 | Markdown 渲染 | H1/H2、段落、列表、待办、行内代码/加粗/链接                                  | 预览渲染展示                                     |
| ED-13 | 待办勾选        | 方框 / 对勾 / 删除线                                              | 展示完成态                                      |
| ED-14 | 代码块         | `terminal` + 语言标 + 复制按钮 + 语法染色                             | 点击复制整段代码                                   |
| ED-15 | 引用块         | 左侧品牌竖线 + 斜体                                                | 展示                                         |
| ED-16 | 文章标签区       | 正文底部标签 chips（如 #日记 #计划 #学习 #深度工作）                          | 展示；非当前标签可点击（hover 态）                       |
| ED-17 | 编辑器状态栏      | 底部条：已自动保存 + 语言(Markdown) + 编码(UTF-8) + 行/列 + 字数 + 当前语言(中文) | 展示；保存状态随编辑变化                               |
| ED-32 | 行号显示        | 工具栏按钮控制编辑区源码左侧是否显示行号                                       | 点击切换并持久化；与设置「显示行号」联动                       |
| ED-33 | 查找（Ctrl+F）  | 当前文档查找：输入即时计数、上/下一跳转并高亮选中匹配                                | `Ctrl+F` 打开；Enter/Shift+Enter 下/上一个；Esc 关闭 |
| ED-34 | 替换（Ctrl+R）  | 当前文档替换：替换当前匹配 / 全部替换                                       | `Ctrl+R` 打开并显示替换行；Enter 替换下一处              |

### 2.3 右侧面板（侧边面板 280px）

| 编号    | 功能点  | 说明                                                           | 交互                  |
| ----- | ---- | ------------------------------------------------------------ | ------------------- |
| ED-18 | 面板头部 | 标题「侧边面板」+「展开全部」`chevrons-down-up` +「关闭面板」`panel-right-close` | 点击展开/关闭面板           |
| ED-19 | 大纲   | 当前笔记标题结构列表，H1 高亮、子标题缩进                                       | 点击跳转对应标题；可折叠/展开     |
| ED-20 | 反向链接 | 引用当前笔记的其它笔记卡片，含文件名、相对日期、正文预览（内链 `[[xx]]` 高亮）+ 计数徽标           | 点击卡片打开对应笔记；头部可折叠/展开 |
| ED-21 | 标签云  | 全部标签 chips + 计数徽标，字号/深浅体现热度                                  | 展示；点击筛选（占位）         |

***

## 3. 命令面板（command palette，`Ctrl+P / Cmd+P`）

| 编号     | 功能点     | 说明               | 交互 / 快捷键                  |
| ------ | ------- | ---------------- | ------------------------- |
| CMD-01 | 打开/关闭   | 遮罩 + 浮窗          | `Ctrl+P` 打开；`ESC` 或点击遮罩关闭 |
| CMD-02 | 命令搜索    | 占位“输入命令或搜索笔记...” | 输入实时过滤命令列表                |
| CMD-03 | 新建笔记    | 最近使用组            | `⌘N` / 选择执行               |
| CMD-04 | 打开笔记    | 最近使用组            | `⌘O` / 选择执行               |
| CMD-05 | 搜索笔记    | 文件操作组            | `⌘F` / 选择执行               |
| CMD-06 | 导出为 PDF | 文件操作组            | 选择执行                      |
| CMD-07 | 打开图谱视图  | 导航组              | `⌘G` / 选择执行，跳转图谱页         |
| CMD-08 | 浏览插件市场  | 插件组              | 选择执行，跳转插件页                |
| CMD-09 | 切换主题    | 插件组              | 选择执行（深/浅主题切换）             |
| CMD-10 | 同步设置    | 插件组              | 选择执行                      |
| CMD-11 | 键盘导航    | 上下选中、Enter 执行    | `↑↓` 导航、`↵` 选择、`esc` 关闭   |
| CMD-12 | 结果计数    | 底部状态栏（如 8 个结果）   | 随过滤更新                     |

***

## 4. 图谱视图（graph）

### 4.1 左侧过滤面板（220px）

| 编号    | 功能点    | 说明                        | 交互          |
| ----- | ------ | ------------------------- | ----------- |
| GR-01 | 标签过滤   | 勾选列表，圆点+名称+计数，头显“已选/总数”   | 勾选过滤图谱      |
| GR-02 | 连接深度   | 1层/2层/3层 + 滑块视觉 + `2层` 徽标 | 点击层数或拖动滑块调整 |
| GR-03 | 节点类型过滤 | 笔记 / 文件夹 / 标签，带图标+计数      | 勾选过滤节点      |
| GR-04 | 重置     | `rotate-ccw` 按钮           | 一键还原所有过滤条件  |

### 4.2 顶部工具栏（40px）

| 编号    | 功能点  | 说明                         | 交互       |
| ----- | ---- | -------------------------- | -------- |
| GR-05 | 布局切换 | 力导向 / 层级                   | 点击切换布局算法 |
| GR-06 | 节点统计 | 节点 10 · 连接 14              | 展示       |
| GR-07 | 缩放控制 | `-` 缩小 / % / `+` 放大 / 重置视图 | 点击调整画布缩放 |

### 4.3 SVG 画布

| 编号    | 功能点  | 说明                             | 交互               |
| ----- | ---- | ------------------------------ | ---------------- |
| GR-08 | 节点展示 | 渐变节点（中心/普通/高亮）+ 名称标签，节点可带连接数徽标 | 展示，节点对应笔记/文件夹/标签 |
| GR-09 | 连线展示 | 一级粗线、二级弱线                      | 展示笔记间关联          |
| GR-10 | 脉冲动画 | 中心及侧节点放射波纹                     | 展示（装饰）           |
| GR-11 | 图例   | 左下角：当前节点 / 选中 / 普通节点 颜色示例      | 展示               |
| GR-12 | 操作提示 | 右下角「拖拽平移 · 滚轮缩放」               | 展示提示             |

### 4.4 右侧节点详情面板（260px）

| 编号    | 功能点    | 说明                               | 交互            |
| ----- | ------ | -------------------------------- | ------------- |
| GR-13 | 面板标题   | 「节点详情」+ `info` 图标                | 展示            |
| GR-14 | 节点名称卡片 | 图标块 + 名称 + 分类徽标 + 连接数            | 展示选中节点        |
| GR-15 | 基本信息   | 创建时间 / 修改时间 / 字数                 | 展示            |
| GR-16 | 标签     | 节点标签 chips（带色圆点）                 | 展示            |
| GR-17 | 连接列表   | 关联节点列表，各带图标 + 连接方向箭头(入/出) + 计数徽标 | 点击列表项；hover 态 |
| GR-18 | 打开笔记   | 底部主按钮「打开笔记」`external-link`       | 点击打开对应笔记      |

***

## 5. 插件市场（plugins）

### 5.1 头部与筛选

| 编号    | 功能点     | 说明                                      | 交互            |
| ----- | ------- | --------------------------------------- | ------------- |
| PL-01 | 页标题     | 插件市场 + 副标题                              | 展示            |
| PL-02 | 管理已安装插件 | `package-check` 按钮                      | 跳转已安装插件管理     |
| PL-03 | 搜索插件    | 占位“搜索插件...”                             | 按名称/描述过滤卡片    |
| PL-04 | 排序      | 最热门 / 最新 / 评分最高                         | 下拉切换排序        |
| PL-05 | 分类过滤    | 全部 / 编辑器增强 / 主题外观 / 数据同步 / 生产力工具 / 集成服务 | Chip 切换，选中态高亮 |

### 5.2 插件卡片

| 编号    | 功能点   | 说明             | 交互            |
| ----- | ----- | -------------- | ------------- |
| PL-06 | 卡片信息  | 图标块、名称、作者、两行描述 | 展示            |
| PL-07 | 量化指标  | 下载量 + 评分       | 展示            |
| PL-08 | 安装/卸载 | 安装 或 已安装（可卸载）  | 点击安装；已安装态展示对勾 |

### 5.3 详情抽屉

| 编号    | 功能点        | 说明                                         | 交互                                       |
| ----- | ---------- | ------------------------------------------ | ---------------------------------------- |
| PL-09 | 详情抽屉       | 右侧滑出详情面板（默认折叠）                             | 点击插件卡片展开详情（占位）                           |
| PL-10 | 插件左侧图标注册   | 已安装插件可向左侧 Ribbon 栏注册自定义图标入口，点击切换到插件视图      | 插件 manifest 声明 `ribbonIcon`；注册后与原生导航并列显示 |
| PL-11 | 插件右键菜单注入   | 插件可向编辑器文件树、编辑区等位置注入自定义右键菜单项                | 插件 manifest 声明 `contextMenus`；与原生菜单合并显示  |
| PL-12 | 插件被命令面板搜索到 | 已安装插件的命令（如「加密当前文件」）自动注册到命令面板，可被 Ctrl+P 搜索到 | 插件 manifest 声明 `commands`；注册后出现在命令面板对应分组 |

> 示例插件数据：Calendar(Kanban作者NoteApp Team)、Kanban(ProductivityLabs)、Excalidraw(Zsolt Viczián)、Git Sync(Vinadon)、Minimal Theme(Stephan Ango)、Mind Map(Vincent Le)、Notion Sync(Cloud Studio)、Code Highlight(Daniel W. P.)、Dark Mode Pro(hasegawa) 等，含分类 cat、下载量、评分。

***

## 6. 设置（settings）

### 6.1 分类侧边栏

| 编号    | 功能点  | 说明                                         | 交互         |
| ----- | ---- | ------------------------------------------ | ---------- |
| ST-01 | 分类导航 | 外观 / 编辑器 / 快捷键 / 同步与备份 / 隐私与安全 / 插件管理 / 关于 | 点击切换，选中态高亮 |

### 6.2 外观面板（示例）

| 编号    | 功能点  | 说明                                         | 交互                   |
| ----- | ---- | ------------------------------------------ | -------------------- |
| ST-02 | 主题模式 | 深色 / 浅色 / 跟随系统                             | 卡片选择，选中显示对勾          |
| ST-03 | 强调色  | 6 色圆点                                      | 点击切换主色调（紫/蓝/绿/橙/红/粉） |
| ST-04 | 字体大小 | 滑块 14–18px，默认 15px                         | 拖动调整正文字号             |
| ST-05 | 字体族  | Inter / Noto Sans SC / 系统默认                | 下拉选择                 |
| ST-06 | 代码字体 | JetBrains Mono / Fira Code / Cascadia Code | 下拉选择                 |
| ST-07 | 界面密度 | 紧凑 / 标准 / 舒适                               | 卡片选择，选中态高亮           |
| ST-08 | 显示行号 | toggle 开关                                  | 开关控制编辑区/代码块行号        |
| ST-09 | 自动换行 | toggle 开关                                  | 开关控制长文本折行显示          |
| ST-10 | 减少动画 | toggle 开关                                  | 开关降低界面过渡与动画强度        |

> 其余分类（编辑器、快捷键、同步与备份、隐私与安全、插件管理、关于、AI 问答）：当前为导航入口，详细设置项随需求补充。

***

## 7. AI 问答（ai）

### 7.1 AI 问答视图

| 编号    | 功能点      | 说明                                                                               | 交互 / 数据                                 |
| ----- | -------- | -------------------------------------------------------------------------------- | --------------------------------------- |
| AI-01 | 视图入口     | Ribbon 新增「AI 问答」（brain 图标）                                                       | 点击切换视图                                  |
| AI-02 | 知识库索引    | 将当前知识库全部 .md 切分并向量化（本地 ONNX 嵌入），索引按知识库名分文件存储，块携带元数据（知识库名/笔记 frontmatter id/块 id） | 首次进入/手动重建；展示索引状态（片段数）                   |
| AI-03 | 语义问答     | 输入问题 → 本地检索相关片段 → 远程/本地大模型流式回答                                                   | Enter 发送；SSE 流式显示                       |
| AI-04 | 回答来源     | 展示检索到的笔记片段（文件名 + 相似度 + 预览）                                                       | 点击片段打开对应笔记                              |
| AI-05 | 多轮会话     | 同一会话追问携带历史上下文                                                                    | 会话内消息累积                                 |
| AI-06 | 会话管理     | 新建会话 / 清空当前会话                                                                    | 按钮操作                                    |
| AI-08 | 模型选择器    | 底部输入区左侧下拉切换生成模型（值=模型 id，由顶部移入输入区）                                                | 下拉选择；切换即保存 currentModelId               |
| AI-09 | 多模型配置    | 支持配置多个生成模型（本地 Ollama / 远程 OpenAI 兼容）                                             | 设置页模型列表添加/编辑/删除；问答页底部切换                 |
| AI-10 | 会话侧栏     | 左侧边栏展示会话列表，支持多会话切换/删除与展开/收起                                                      | 点击切换会话；悬浮 × 删除；收起后左缘把手展开                |
| AI-11 | 索引增量更新   | 新建/保存/删除笔记自动触发该笔记块的增量索引更新，不影响库内其他笔记（按知识库名·笔记 frontmatter id·块 id 定位）             | 保存/删除笔记时后台触发                            |
| AI-12 | 查看索引     | 编辑区顶部「查看索引」按钮在编辑区底部内联展开当前笔记的分块索引（笔记 id / 块号 / 块 id / 文本片段）                       | 点击按钮展开/收起；切换笔记时随当前笔记刷新；无索引时提示去 AI 问答页重建 |
| AI-13 | 索引库管理    | AI 页顶部「重建索引」改为「索引库」下拉：列出所有已有索引的知识库；目录仍存在→显示「重建」，目录已移除但残留索引→显示「删除」；底部提供「重建当前知识库」  | 点击按钮开闭下拉；重建/删除行内操作并实时刷新                 |
| AI-14 | 嵌入模型自动检测 | 启动时自动检测本地常用模型管理器（Ollama / LM Studio）是否已下载嵌入模型                                    | 设置页/AI 页显示检测状态（已检测到 / 未检测到）             |
| AI-15 | 嵌入模型下载按钮 | 未检测到本地嵌入模型时，提供一键下载/拉取按钮（Ollama 走 `ollama pull`，其他管理器走对应命令）                       | 点击按钮执行下载；显示下载进度；完成后自动更新状态               |

### 7.2 设置 - AI 问答分类

| 编号    | 功能点      | 说明                                                                               | 交互                  |
| ----- | -------- | -------------------------------------------------------------------------------- | ------------------- |
| ST-13 | 本地嵌入模型   | 配置向量化模型路径（默认 models/sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2） | 输入路径                |
| ST-14 | 本地重排序模型  | 配置重排序模型路径（默认 models/bge-reranker-v2-m3）                                          | 输入路径                |
| ST-15 | 生成模型列表   | 列表展示已配置生成模型（供应商 + 模型名称 + 当前标记）                                                   | 添加/编辑/删除按钮          |
| ST-16 | 添加/编辑弹层  | 提供方 + 接口地址 + API Key + 模型名称                                                      | 弹层表单保存/取消           |
| ST-17 | 模型与服务状态  | 嵌入模型加载状态、索引状态                                                                    | 展示；重建索引按钮           |
| ST-18 | 模型下载目录设置 | 嵌入模型/重排序模型的下载存储目录可配置（默认随应用 models/ 目录）                                           | 目录选择器；修改后提示是否迁移已有模型 |

***

## 8. 应用安全

| 编号     | 功能点    | 说明                                          | 交互 / 数据                           |
| ------ | ------ | ------------------------------------------- | --------------------------------- |
| SEC-01 | 启动密码   | 首次启动时设置密码；每次打开应用需输入密码才能进入主界面                | 密码输入框 + 确认；密码本地加密存储（bcrypt 或等效方案） |
| SEC-02 | 超时锁定遮罩 | 应用长时间未操作（默认 10 分钟，可配置）后，弹出遮罩层，需重新输入密码才能继续使用 | 空闲计时器；遮罩覆盖全屏；密码正确则移除遮罩恢复原界面       |

***

## 9. 加密插件（待实现 · 示例插件）

| 编号     | 功能点     | 说明                                                                           | 交互                                     |
| ------ | ------- | ---------------------------------------------------------------------------- | -------------------------------------- |
| ENC-01 | 加密/解密文件 | 基于完善后的插件体系（PL-10/11/12），提供文件加密能力：右键菜单「加密此文件」、命令面板搜索「加密」、设置密码后对 .md 文件 AES 加密 | 右键菜单 / 命令面板触发；密码输入；加密后原文件变为密文格式；支持解密恢复 |

***

## 10. Git 同步

| 编号     | 功能点    | 说明                                                                     | 交互                      |
| ------ | ------ | ---------------------------------------------------------------------- | ----------------------- |
| GIT-01 | 初始化仓库  | 当前知识库一键 `git init`，自动将 .md 文件加入版本控制                                    | 设置页「同步与备份」分类 → Git 同步入口 |
| GIT-02 | 自动提交   | 笔记变更时自动执行 `git add . && git commit -m "auto: update YYYY-MM-DD HH:mm"` | 防抖提交，避免高频小提交            |
| GIT-03 | 远程仓库配置 | 配置远程仓库地址（https/ssh），支持多远程                                              | 设置页表单；保存时测试连接           |
| GIT-04 | 推送/拉取  | 手动或自动将本地变更推送到远程，支持从远程拉取最新内容                                            | 按钮操作；冲突时提示用户手动处理        |

