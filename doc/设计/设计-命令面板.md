# 第二脑 · 设计文档：命令面板

> 对应需求模块 `需求-命令面板.md`（CMD-xx）。

遮罩 + 浮窗结构；`Ctrl+P` 打开、`ESC` 或点击遮罩关闭；输入实时过滤命令 / 笔记列表；`↑↓` 导航、`Enter` 执行、`esc` 关闭；底部状态栏显示结果计数。
命令分组：

- 最近使用：新建笔记（`⌘N`）、打开笔记（`⌘O`）；

- 文件操作：搜索笔记（`⌘F`）、导出为 PDF；

- 导航：打开图谱视图（`⌘G`）、浏览插件市场；

- 插件：切换主题、同步设置。
