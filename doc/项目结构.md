# 项目结构（第二脑 / second-brain）

> **本文档为项目目录结构的唯一权威来源。** 结构变更时同步更新本文档，并遵循 `AGENTS.md`「文档同步规则」。
> 原文曾内嵌于 `.hermes/project.md`，现独立成文并迁至 `doc/`（2026-09-04）。

## 项目概览

- **项目名称**：第二脑（工程目录 `second-brain`）—— Obsidian 风格的笔记应用

- **形态**：桌面程序（Electron 主）+ 网页版（调试辅助）

- **技术栈**：Electron + 原生 HTML / CSS / Tailwind（本地化）/ Lucide 图标 / hash 路由 SPA

- **本地 AI**：`@xenova/transformers`（分词/嵌入）+ `onnxruntime-node`（重排序），本地向量索引与语义检索

- **设计基调**：单品牌色 Violet `#7C3AED`，深/浅双主题，Design Token 驱动

## 目录结构

```
根目录（项目根）
├── AGENTS.md        # 只放「读取文件」导航 + 控制 AGENTS 的规则（唯一入口）
├── .hermes/         # 项目规则/编码规范落点（不承载设计/进度正文）
│   ├── project.md         # 项目信息（目录结构已迁至 doc/项目结构.md）
│   ├── coding-style.md    # 编码风格、开发与测试规范
│   ├── design.md          # 重定向 → doc/设计文档.md + doc/功能需求文档.md
│   └── progress.md        # 重定向 → doc/开发进度.md
├── doc/             # 设计与需求、进度、结构的唯一权威来源
│   ├── 设计文档.md        # 视觉/布局权威（变更界面前必查）
│   ├── 功能需求文档.md     # 按页面枚举全部功能点（实现前必查，不遗漏）
│   ├── 开发进度.md        # 已完成/待办/常用命令
│   └── 项目结构.md        # 本文件：目录结构权威
├── design/
│   └── pages/            # 原始视觉设计稿（HTML）：主编辑器/命令面板/图谱/插件市场/设置
└── second-brain/         # 应用工程
    ├── index.html            # 应用壳：共享标题栏 + Ribbon + 命令面板覆盖层 + 视图容器
    ├── main.js               # Electron 主进程：创建窗口、note:// 协议、窗口控制 IPC
    ├── preload.js            # Preload：contextIsolation 暴露 window.noteDesktop
    ├── ai-engine.js          # AI 引擎（主进程，CommonJS）：本地 ONNX 嵌入/重排序、
    │                         #     远程大模型（OpenAI 兼容）+ 本地 Ollama 流式生成
    ├── cdp.mjs               # 最小 CDP 客户端（临时诊断用，ESM，仅调试）
    ├── package.json          # 依赖、scripts（npm start 启动；npm run dist 打包）
    ├── package-lock.json     # 依赖锁定
    ├── start-desktop.bat     # 双击启动桌面版（首次自动 npm install）
    ├── start-server.bat      # 网页版调试服务器（python http.server，127.0.0.1:8000）
    ├── 打开桌面版.vbs          # 无窗口启动器：隐藏 cmd 调 start-desktop.bat（避免关闭控制台退出程序）
    ├── css/
    │   ├── base.css          # 设计令牌（Token）：品牌色/中性色/圆角/深/浅主题变量
    │   └── app.css           # 组件样式 + 响应式（含 .window-controls 窗口按钮）
    ├── js/
    │   ├── app-core.js … app-layout.js # 应用逻辑（按模块拆为 core/note/editor/graph/plugins/settings/ai/vault/editor-ctx/layout，由 index.html 依次引入）
    │   └── vendor/           # 本地化第三方库（Tailwind、Lucide），禁止 CDN
    ├── views/                # 各视图页面（hash 路由 loadView 动态加载）
    │   ├── editor.html       # 主编辑器：文件树 + Markdown 编辑/预览/分屏 + 侧边面板
    │   ├── graph.html        # 图谱：SVG 画布 + 过滤/缩放/深度
    │   ├── plugins.html      # 插件市场：分类 + 搜索 + 安装
    │   ├── settings.html     # 设置：外观/编辑器/快捷键 + AI 问答分类
    │   └── ai.html           # AI 问答：知识库语义检索 + 多轮流式问答
    ├── dist/                 # electron-builder 打包产物（NSIS 安装版 + 便携版 exe），勿手动编辑
    └── node_modules/         # npm 依赖，勿手动编辑（第三方自带测试文件勿删）
```

## 目录/文件职责与注意事项

| 路径                           | 职责              | 注意                                                  |
| ---------------------------- | --------------- | --------------------------------------------------- |
| `doc/`                       | 设计、需求、进度、结构唯一权威 | 变更界面前必查设计/需求文档；结构变更同步更新本文件                          |
| `.hermes/`                   | 规则导览 + 编码规范     | 仅 `project.md`/`coding-style.md` 承载正文，其余重定向到 `doc/` |
| `second-brain/views/`        | 各 hash 视图页面     | 新增视图须同步 Ribbon 导航与 `js/app-*.js` 路由                      |
| `second-brain/js/vendor/`    | 本地化第三方库         | 禁止从 CDN 加载，须在离线可用                                   |
| `second-brain/js/app-*.js`   | 全部视图交互 + 路由（按模块拆分） | 新增功能点先查需求/设计文档；修改视图逻辑按模块定位对应文件            |
| `second-brain/main.js`       | Electron 主进程    | 窗口、note:// 协议、窗口控制 IPC                              |
| `second-brain/ai-engine.js`  | AI 引擎（主进程共用）    | 本地 ONNX + 远程/本地大模型，被 IPC 调用                         |
| `second-brain/dist/`         | 打包产物            | 勿手动编辑；`npm run dist` 生成                             |
| `second-brain/node_modules/` | 依赖              | 勿手动编辑；内含第三方自带测试文件，勿清理                               |

## 关键要点

- 应用为 **hash 路由 SPA**：`index.html` 依次引入 `js/app-*.js`，其中 `app-layout.js` 的 `loadView()` 按 hash 动态加载 `views/*.html`。

- **桌面版**：`electron .`（`npm start`）启动，`main.js` 加载 `index.html` 并启用 `note://` 协议。

- **网页版调试**：`start-server.bat` 起本地 http 服务器，访问 `http://127.0.0.1:8000/index.html`。

- **打包**：`npm run dist`，产物在 `second-brain/dist/`。

- `NoteApp` 已于 2026-09-02 更名为 `second-brain`，命令/路径一律用新目录名。

