# AGENTS.md — 项目智能体入口规则

> 本文件只负责：**告诉 AGENTS 按最小路径定位要读的文件**，以及**控制 AGENTS 行为的基本规则**。
> 具体的项目规则导览见 `.hermes/`，设计与需求/进度的权威正文在根目录 `doc/`。

## 每次任务开始

**先读 `doc/读取路由.md`（唯一路由，决定本次该读哪些文件）**，按其「任务类型 / 模块映射」只读**最小必要文件集**，不强制通读以下全部文档，以节省 token。

路由已覆盖的按需文件（任务命中时再读，勿每次全读）：

- `.hermes/project.md` —— 项目信息、技术栈（目录结构权威正文在 `doc/项目结构.md`）
- `.hermes/coding-style.md` —— 编码风格、开发与测试规范（编码任务时读）
- `doc/需求/需求-<模块>.md` + `doc/设计/设计-<模块>.md` —— 功能点正文/设计正文（**功能变更前必查对应模块**）
- `doc/开发进度.md` —— 未完结看板、常用命令（开发/登记任务时读）
- `.hermes/bug-tracking.md` + `doc/历史Bug记录.md` —— bug 语义时读
- `doc/项目结构.md` —— 只查结构时读

## 权威来源约定

- **项目规则导览**看 `.hermes/`（project.md / coding-style.md）。

- **`.hermes/`** **的** **`design.md`** **/** **`progress.md`** **/** **`bug-tracking.md`** **职责一致**：都只在 `.hermes/` 协定「规则 + 模板/格式口径」，不承载实际正文与数据；实际内容一律落地 `doc/`：

  - `design.md` → 权威正文 `doc/设计文档.md` + `doc/功能需求文档.md`

  - `progress.md` → 权威正文 `doc/开发进度.md`

  - `bug-tracking.md` → 实际记录 `doc/历史Bug记录.md`

- **因此设计与功能需求、开发进度、历史 Bug 的实际落地均为** **`doc/`**：`.hermes/` 只存各文档的规则与模板，不存实际内容。

## 控制 AGENTS 的基础规则

- 实现/修改**任何功能前**，必须先查询 `doc/功能需求文档.md` + `doc/设计文档.md` 与对应设计稿（`design/pages/*.html`），再编码，不要遗漏功能点。

- 只做被要求的事：不扩散已测功能、不大面调整、不主动新增文档/文件。

- 只测本次新增/修改的功能；修改文件需在回复中展示**前后对比**。

- 若 `.hermes/` 内容与本文件冲突，以 `.hermes/` 和 `doc/` 的最新结构为准。

- 每次完成开发后，更新 `doc/开发进度.md`。

- **文档同步规则**：新增/修改/移动任何文档或结构时，必须同步更新所有引用它、涉及它的文件（如本文档的导航、`.hermes/*` 的重定向、`doc/开发进度.md` 的记录、`project.md` 的目录结构），避免多份文档脱节。

- **功能追踪规则**：新增模块或功能点，先登记到 `doc/功能需求文档.md`，并在 `doc/开发进度.md` 跟踪表中新增一行（状态=计划）；每推进一档状态（计划→设计→编码→开发完成→测试完成→完成）就更新该行状态与记录。

- **添加需求仅完善文档规则**：当用户只说「添加/新增需求」而不要求实现时，只补充完善对应文档（登记到 `doc/功能需求文档.md`，并在 `doc/开发进度.md` 跟踪表新增一行），**不编写或修改任何功能代码**，回复「已记录」即可。

- **已完成归档规则**：`doc/开发进度.md` 的「未完结看板」只保留**未完结**条目；功能点推进到**「完成」**（或按模块惯例达到归档口径，如「测试完成」）时，必须把该行**从看板移除**，移入对应已完结历史文件 `doc/进度/进度-<模块>.md`（各模块历史文件按列 `编号|功能点|详情|状态|开始时间|完成时间|记录` 追加），并同步更新看板「已完结历史」模块导航（在需要的场景补链接）。不得把已完成条目留在看板。新需求从需求文档登记开始走「功能追踪规则」，完成后走本归档规则。

- **历史 Bug 追踪规则**：每次对话先判断用户表述是否包含 bug 语义（「报错」「bug」「异常」「崩溃」「坏了」等），包含则按 `.hermes/bug-tracking.md` 的规则与模板登记到 `doc/历史Bug记录.md`（规则/模板权威在 `.hermes/bug-tracking.md`，记录落点在 `doc/历史Bug记录.md`）。登记内容必须含**状态**与**处理方法**（状态枚举见规范文件）。**修复完成后**须把根因提炼为通用约束回填到 `.hermes/bug-tracking.md` 的「防再犯清单」（CD-编号）；能确定性复现的 bug 须在 `regression.test.js` 补一条回归断言，并在提交前 `npm test` 通过（诊断脚本删、回归脚本保留）。

- **版本发布维护规则**：当用户说「打包发布/发布新版本」等表述时，必须同时维护以下三处，使版本号保持一致并追加变更记录：
  1. `second-brain/package.json` 的 `version` — 提升为新的版本号；
  2. `second-brain/js/changelog.js` 的全局 `CHANGELOG` 数组 **最前** 追加一条新版本（`version` 与 package.json 一致、`date`、`title`、`items` 列出本次新增/变更内容）；
  3. 发布日期按当天日期填写；「设置-关于」页头版本号与「版本历史」会自动读取该数据（取最新一条），无需额外改界面。
  另按「功能追踪规则」在 `doc/功能需求文档.md` 与 `doc/开发进度.md` 登记/推进新版本功能条目。

- \*\*规则记录去向判断规则：当用户说「记录规则」等表述时，先判断该规则的性质，再决定写入 `AGENTS.md` 还是 `.hermes/`：

  - **记录到** **`AGENTS.md`**：属于「控制智能体执行行为 / 任务流程」的规则 —— 如任务入口要读哪些文件、实现前先查文档、只做被要求的事、只测本次改动、`doc/` 写入用 WriteAllLines+MD5 双校验、先拆分需求/bug、功能追踪推进度档位、规则记录去向判断等，即本文件「控制 AGENTS 的基础规则」所涵盖的范畴。

  - **记录到** **`.hermes/`**：属于「项目本身属性」的规则 —— 项目信息 / 技术栈写入 `.hermes/project.md`（目录结构权威正文在 `doc/项目结构.md`），编码风格 / 命名 / 注释规范写入 `.hermes/coding-style.md`，文档职责划分等非智能体执行流程的规则。

  - 若一条规则二者皆涉及（例如既有行为判断又带项目细节），可在 `AGENTS.md` 记录核心行为判断，并在 `.hermes/` 的对应文件补充项目属性细节，避免脱节。

