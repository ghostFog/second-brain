# AGENTS.md — 项目智能体入口规则

> 本文件只负责：**告诉 AGENTS 读取哪些文件**，以及**控制 AGENTS 行为的基本规则**。
> 具体的项目规则导览见 `.hermes/`，设计与需求/进度的权威正文在根目录 `doc/`。

## 每次任务开始必须读取

按顺序读取以下文件，获取项目与规则的完整上下文：

1. `.hermes/project.md` —— 项目信息、技术栈、文档职责划分（目录结构权威正文在 `doc/项目结构.md`）
2. `.hermes/coding-style.md` —— 编码风格、开发与测试规范
3. `doc/设计文档.md` + `doc/功能需求文档.md` —— 设计文档与功能需求（**功能变更前必查**）
4. `doc/开发进度.md` —— 开发进度跟踪表、常用命令

## 权威来源约定

- **项目规则导览**看 `.hermes/`（project.md / coding-style.md）。

- **设计与功能需求、开发进度以** **`doc/`** **为唯一权威来源**；`.hermes/design.md`、`.hermes/progress.md` 仅做重定向，不承载正文。

## 控制 AGENTS 的基础规则

- 实现/修改**任何功能前**，必须先查询 `doc/功能需求文档.md` + `doc/设计文档.md` 与对应设计稿（`design/pages/*.html`），再编码，不要遗漏功能点。

- 只做被要求的事：不扩散已测功能、不大面调整、不主动新增文档/文件。

- 只测本次新增/修改的功能；修改文件需在回复中展示**前后对比**。

- 若 `.hermes/` 内容与本文件冲突，以 `.hermes/` 和 `doc/` 的最新结构为准。

- 每次完成开发后，更新 `doc/开发进度.md`。

- **文档同步规则**：新增/修改/移动任何文档或结构时，必须同步更新所有引用它、涉及它的文件（如本文档的导航、`.hermes/*` 的重定向、`doc/开发进度.md` 的记录、`project.md` 的目录结构），避免多份文档脱节。

- **功能追踪规则**：新增模块或功能点，先登记到 `doc/功能需求文档.md`，并在 `doc/开发进度.md` 跟踪表中新增一行（状态=计划）；每推进一档状态（计划→设计→编码→开发完成→测试完成→完成）就更新该行状态与记录。

- \*\*规则记录去向判断规则：当用户说「记录规则」等表述时，先判断该规则的性质，再决定写入 `AGENTS.md` 还是 `.hermes/`：

  - **记录到** **`AGENTS.md`**：属于「控制智能体执行行为 / 任务流程」的规则 —— 如任务入口要读哪些文件、实现前先查文档、只做被要求的事、只测本次改动、`doc/` 写入用 WriteAllLines+MD5 双校验、先拆分需求/bug、功能追踪推进度档位、规则记录去向判断等，即本文件「控制 AGENTS 的基础规则」所涵盖的范畴。

  - **记录到** **`.hermes/`**：属于「项目本身属性」的规则 —— 项目信息 / 技术栈写入 `.hermes/project.md`（目录结构权威正文在 `doc/项目结构.md`），编码风格 / 命名 / 注释规范写入 `.hermes/coding-style.md`，文档职责划分等非智能体执行流程的规则。

  - 若一条规则二者皆涉及（例如既有行为判断又带项目细节），可在 `AGENTS.md` 记录核心行为判断，并在 `.hermes/` 的对应文件补充项目属性细节，避免脱节。

